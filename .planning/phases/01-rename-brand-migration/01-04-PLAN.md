---
phase: 01-rename-brand-migration
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - scripts/rename-brand.mjs (deleted)
autonomous: false
requirements: [BRAND-05]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "npm run typecheck && npm run lint && npm test && npm run build all pass after every rename plan (01-01, 01-02, 01-03) has landed"
    - "A case-insensitive grep for 'sourceiq', scoped both to git-tracked files and to the full working tree, returns zero hits outside the documented exception list (sourceiq-ux-autoresearch/, procurement-app-autoresearch/, .planning/, sourceiq.db, sourceiq.db-shm, sourceiq.db-wal, .claude/worktrees/)"
    - "No filename anywhere in the tracked or untracked working tree, outside the exception list, contains 'sourceiq'"
  artifacts: []
  key_links:
    - "The dual-scope grep sweep (git-ls-files-scoped + full-working-tree-scoped) closes the blind spot a git-ls-files-only sweep would miss for untracked files, e.g. the docs/brand/ assets renamed in Plan 01-01"
---

<objective>
Close out Phase 1 with the full standing verification suite, a dual-scope repo-wide
BRAND-05 grep sweep (both git-tracked and full-working-tree, to catch untracked files
the tracked-only sweep would miss), a filename-level check, an advisory human check of
the two sandbox-unreadable env files, and removal of the now-disposable
`scripts/rename-brand.mjs` tooling.

Purpose: Confirm the phase's exit criterion — a repo-wide, case-insensitive grep for
"SourceIQ" returns zero unintended hits, with only the two documented `-autoresearch`
directories (plus this phase's own additional, narrowly-scoped, explicitly-justified
exceptions: `.planning/`, the `sourceiq.db*` SQLite files, and the unrelated
`.claude/worktrees/` tooling artifact directory) as exceptions — actually holds, across
every plan in this phase, before Phase 1 is marked complete.
Output: Full verification suite green; dual-scope BRAND-05 sweep clean;
scripts/rename-brand.mjs deleted; human sign-off recorded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-rename-brand-migration/01-CONTEXT.md
@.planning/phases/01-rename-brand-migration/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full verification suite after all three rename plans have landed</name>
  <files>none (verification only)</files>
  <read_first>package.json scripts block</read_first>
  <action>
    Run the project's standing full verification command, in order: `npm run
    typecheck`, `npm run lint`, `npm test`, `npm run build` (per D-08), confirming each
    exits 0 now that Plans 01-01 (bulk scripted rename), 01-02 (lib/agents.ts manual
    security-critical rename), and 01-03 (legal pages and narrative docs manual rename)
    have all landed. This is the first point where every file touched across all three
    prior plans is checked together in one pass.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
    - `npm test` exits 0 (including tests/prompt-injection-defense.test.ts's 7 guard-behavior assertions)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The full verification suite passes with every rename plan in this phase merged.</done>
</task>

<task type="auto">
  <name>Task 2: Run the dual-scope BRAND-05 grep sweep, a filename-level check, and delete scripts/rename-brand.mjs</name>
  <files>scripts/rename-brand.mjs (deleted)</files>
  <read_first>none — this task runs shell commands against the current working tree</read_first>
  <action>
    Run two independent grep sweeps to close the blind spot noted in RESEARCH.md: its
    own recommended sweep command is scoped to `git ls-files`, which is blind to
    untracked files (confirmed during planning: all 6 docs/brand/ image assets were
    untracked before Plan 01-01 renamed them). First, run the git-ls-files-scoped
    sweep exactly as RESEARCH.md recommends, excluding the two documented
    `-autoresearch` directories. Second, run a full-working-tree filesystem grep (not
    limited to git-tracked files) excluding node_modules/, .next/, .git/, .planning/,
    the two `-autoresearch` directories, the three `sourceiq.db*` files, and
    `.claude/worktrees/` — this second sweep is this phase's own addition beyond
    RESEARCH.md's recommendation, specifically to catch any untracked file the first
    sweep would miss. Both sweeps must return zero lines. Then run a filename-level
    check (both `git ls-files` and a full filesystem `find`, same exclusions) confirming
    no filename anywhere in scope still contains "sourceiq". Do not hardcode any
    expected match count into these checks — assert on zero output only, since the
    exact pre-rename occurrence count was never fully reconciled during planning
    (RESEARCH.md's own count and a direct repo check produced two different numbers).
    Finally, delete scripts/rename-brand.mjs — it is disposable one-time tooling, not
    added to package.json's permanent scripts block, and its job is done.
  </action>
  <verify>
    <automated>git ls-files -z | xargs -0 grep -Zlio sourceiq | grep -zv -E '^(sourceiq-ux-autoresearch/|procurement-app-autoresearch/)' | tr '\0' '\n' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The git-ls-files-scoped sweep (excluding the two `-autoresearch` directories) returns zero lines
    - A full-working-tree filesystem grep (excluding node_modules/, .next/, .git/, .planning/, the two `-autoresearch` directories, sourceiq.db/-shm/-wal, .claude/worktrees/) returns zero lines
    - A filename-level check (git ls-files and filesystem find, same exclusions) finds no path containing "sourceiq"
    - scripts/rename-brand.mjs no longer exists in the working tree
  </acceptance_criteria>
  <done>Both grep sweeps and the filename check return zero unintended hits, and scripts/rename-brand.mjs is deleted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human confirms .env files and final sign-off</name>
  <what-built>
    Every scripted and manual rename plan in Phase 1 has landed: the bulk scripted
    pass (01-01), the manual lib/agents.ts security-critical rename (01-02), and the
    manual legal-pages/narrative-docs rename (01-03). The full verification suite
    passes and a dual-scope repo-wide grep sweep confirms zero unintended "SourceIQ"
    hits outside the documented exception list.
  </what-built>
  <how-to-verify>
    This is an advisory, low-severity check on two files Claude could not read directly
    during planning or execution (sandbox denies read access to `.env.local` and
    `.env.example`). Manually open `.env.local` and `.env.example` in this repo and
    confirm neither contains a stale "sourceiq"-derived default value (e.g. a cookie
    name prefix, a storage bucket name, a default email-from address, or a comment
    referencing the old brand). If either file contains such a reference, update it to
    "sourcegpt" manually — this is not expected to block Phase 1 completion since these
    files are local/environment-specific and outside the scripted/manual rename scope
    already covered, but should be confirmed clean before considering the rebrand fully
    complete. Also do a final skim of `npm run build`'s output and the dual-scope grep
    results from Task 2 to confirm no unexpected exception was silently introduced.
  </how-to-verify>
  <resume-signal>Type "approved" once .env.local/.env.example are confirmed clean (or fixed), or describe what was found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git-ls-files-scoped grep → full working tree | A tracked-files-only sweep is structurally blind to untracked files (confirmed gap: the 6 docs/brand/ assets were untracked pre-rename) |
| Claude's sandbox → .env.local / .env.example | Claude cannot read these files directly this phase; only a human can confirm their contents are clean |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04-01 | Tampering | BRAND-05 final grep sweep | medium | mitigate | Dual-scope sweep (git-ls-files-scoped per RESEARCH.md's own recommendation, plus a full-working-tree filesystem scope added specifically to catch untracked files) instead of relying on a single tracked-files-only sweep |
| T-01-04-02 | Information Disclosure | .env.local / .env.example (unreadable by Claude's sandbox) | low | mitigate | Explicit human checkpoint delegated for exactly these two files, since Claude cannot verify them directly |

</threat_model>

<verification>
1. `npm run typecheck && npm run lint && npm test && npm run build` all pass (Task 1).
2. Git-ls-files-scoped grep sweep for case-insensitive "sourceiq" (excluding the two `-autoresearch` dirs) returns zero lines.
3. Full-working-tree filesystem grep sweep (excluding node_modules/, .next/, .git/, .planning/, the two `-autoresearch` dirs, sourceiq.db*, .claude/worktrees/) returns zero lines.
4. Filename-level check finds no remaining "sourceiq" path outside the exception list.
5. scripts/rename-brand.mjs is deleted.
6. Human confirms .env.local/.env.example are clean.
</verification>

<success_criteria>
Phase 1's exit criterion holds: a repo-wide, case-insensitive grep for "SourceIQ"
returns zero unintended hits (both git-tracked and full-working-tree scopes), scoped
only to the documented exception list; the full verification suite is green; the
disposable rename script is removed; and a human has signed off on the two
sandbox-unreadable env files.
</success_criteria>

<output>
Create `.planning/phases/01-rename-brand-migration/01-04-SUMMARY.md` when done
</output>
