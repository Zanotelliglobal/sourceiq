---
phase: 01-rename-brand-migration
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/agents.ts
  - tests/prompt-injection-defense.test.ts
autonomous: false
requirements: [BRAND-03, BRAND-04]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "lib/agents.ts's INJECTION_DEFENSE anti-impersonation clause and the identityRules non-disclosure clause read 'SourceGPT' and still refuse to reveal the buyer's identity / the platform's identity in anonymous outreach, confirmed by a human, per BRAND-03"
    - "tests/prompt-injection-defense.test.ts's 7 guard-behavior assertions are unmodified and still pass; only its 2 comment-line brand mentions changed, per BRAND-04"
  artifacts:
    - lib/agents.ts (manually edited, all ~17 literal "SourceIQ" occurrences renamed one at a time)
    - tests/prompt-injection-defense.test.ts (comment lines 7 and 53 only)
  key_links:
    - "identityRules' anonymous-outreach branch ('Do NOT reveal the buyer's identity') and disclosed-outreach branch ('Do NOT mention SourceGPT or any intermediary') both still gate on the same `disclosed` boolean after the rename — the conditional's branching logic itself must not change, only the literal brand word inside each branch's text"
---

<objective>
Manually — one occurrence at a time, never via the bulk script from Plan 01-01 — rename
every literal "SourceIQ" brand mention inside `lib/agents.ts`'s agent system prompts,
with special care around the two security-critical clauses: the INJECTION_DEFENSE
anti-impersonation clause and the outreach `identityRules` non-disclosure clause. Each
clause's guard behavior (the model still refuses to reveal the buyer's or the
platform's identity) is explicitly re-verified after the swap, not assumed from "tests
still pass." In the same pass, manually confirm and update the two incidental
brand-string comment mentions in `tests/prompt-injection-defense.test.ts`.

Purpose: This is the single highest-severity item in the phase per RESEARCH.md's
Critical Pitfalls — a blind scripted replace risks silently weakening a
prompt-injection/anti-impersonation guard that protects buyer identity and platform
identity in live outreach emails to real suppliers. BRAND-03 and BRAND-04 both require
human verification, not automation, for exactly this reason.
Output: `lib/agents.ts` and `tests/prompt-injection-defense.test.ts` renamed with guard
behavior confirmed unchanged by a human.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-rename-brand-migration/01-CONTEXT.md
@.planning/phases/01-rename-brand-migration/01-PATTERNS.md
@.planning/phases/01-rename-brand-migration/01-RESEARCH.md
@lib/agents.ts
@tests/prompt-injection-defense.test.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide guard-behavior verification depth</name>
  <decision>How deep should guard-behavior verification go after manually renaming lib/agents.ts's INJECTION_DEFENSE and identityRules clauses?</decision>
  <context>
    CONTEXT.md's D-04 locks the *mechanism* (manual, one-at-a-time rename, not a blind
    script) but does not specify the *depth* of post-edit verification. lib/agents.ts's
    identityRules conditional currently reads: for disclosed outreach, "Do NOT mention
    SourceIQ or any intermediary"; for anonymous outreach, "Do NOT reveal the buyer's
    identity (SourceIQ acts as intermediary)." Both clauses must still functionally
    suppress the same information after the rename. Static read-through confirms the
    prompt text is correct; it does not confirm the live model still obeys the
    instruction post-edit. A live smoke test costs one Anthropic API call and a few
    minutes but requires ANTHROPIC_API_KEY to be set in the environment.
  </context>
  <options>
    <option id="static-only">
      <name>Static read-through only</name>
      <pros>No API cost, no environment dependency, faster; the edit is a literal one-word substitution inside an unchanged sentence structure, so semantic risk is genuinely low</pros>
      <cons>Does not empirically confirm the live model still honors the guard after the edit — relies entirely on the reasoning that the sentence structure and branching logic are unchanged</cons>
    </option>
    <option id="static-plus-live-smoke-test">
      <name>Static read-through plus a live Anthropic API smoke-test probe</name>
      <pros>Empirically confirms the model still refuses to reveal buyer/platform identity post-edit, closing the "tests green but semantics wrong" gap RESEARCH.md flags as the phase's highest-severity risk</pros>
      <cons>Requires ANTHROPIC_API_KEY in the environment; costs one live API call; adds a few minutes to this plan's execution</cons>
    </option>
  </options>
  <resume-signal>Select: static-only or static-plus-live-smoke-test</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Manually rename all literal "SourceIQ" occurrences in lib/agents.ts and the 2 comment mentions in tests/prompt-injection-defense.test.ts</name>
  <files>lib/agents.ts, tests/prompt-injection-defense.test.ts</files>
  <precondition>If "static-plus-live-smoke-test" was selected in Task 1, ANTHROPIC_API_KEY must be set in the environment before running this task's verification step.</precondition>
  <read_first>lib/agents.ts lines 28-92 (INJECTION_DEFENSE constant and its explanatory comment), lib/agents.ts lines 985-1014 (BuyerIdentity type, identityRules conditional, runOutreachAgent prompt header), tests/prompt-injection-defense.test.ts (all 69 lines, especially the functionBody() helper and lines 7/53)</read_first>
  <action>
    Rename each of the approximately 17 literal "SourceIQ" occurrences in lib/agents.ts
    one at a time — do not run any scripted find/replace on this file. The occurrences
    span: the INJECTION_DEFENSE constant's explanatory comment block, twelve
    "You are SourceIQ's/the SourceIQ ... Agent" system-prompt headers across the
    classifier, filter-mapper, orchestrator, quick-scan, targeted-verification,
    qualifier (×2), enricher, contact-discovery, outreach (×2), and reply-classifier
    agents, and the two security-critical identityRules lines: "Do NOT mention
    SourceIQ or any intermediary" (disclosed-outreach branch) and "Do NOT reveal the
    buyer's identity (SourceIQ acts as intermediary)" (anonymous-outreach branch). For
    every occurrence, swap only the literal brand word — do not alter surrounding
    grammar, punctuation, capitalization pattern, or the identityRules conditional's
    branching structure (the `disclosed` boolean check itself must be byte-identical
    before and after). After the edit, re-read the full identityRules block and the
    INJECTION_DEFENSE constant end-to-end to confirm no clause's meaning shifted.

    In the same pass, in tests/prompt-injection-defense.test.ts, update the two
    incidental brand-string mentions at lines 7 and 53 (both inside comments, not
    assertions) from "SourceIQ" to "SourceGPT" — confirm via the functionBody() helper's
    logic and the file's 7 guard-behavior assertions that no assertion string or
    exported-function-name lookup references the literal brand word (none currently do;
    this file's brand mentions are load-bearing for readability only, per BRAND-04),
    so only the two comment lines change.

    Then, per the option selected in Task 1: if "static-only," perform a final manual
    read-through of the edited identityRules and INJECTION_DEFENSE blocks confirming
    the guard's stated behavior (suppress buyer identity in anonymous mode, suppress
    platform/intermediary name in disclosed mode) is textually unchanged apart from the
    brand word. If "static-plus-live-smoke-test," additionally invoke runOutreachAgent
    once with a disclosed buyer identity and once with an anonymous buyer identity
    (using a real or realistic test supplier), and manually inspect both generated
    email bodies to confirm neither leaks the suppressed information.
  </action>
  <verify>
    <automated>npm test -- tests/prompt-injection-defense.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git diff lib/agents.ts` shows exactly the brand-word substitutions described above, with zero changes to sentence structure, punctuation, or the `disclosed` conditional's branching logic
    - `git diff tests/prompt-injection-defense.test.ts` shows changes only on comment lines 7 and 53, zero changes to any assertion or the functionBody() helper
    - `npm test -- tests/prompt-injection-defense.test.ts` passes all 7 guard-behavior assertions
    - If static-plus-live-smoke-test was selected: both generated outreach emails (disclosed and anonymous) were manually inspected and neither leaks the suppressed identity information
  </acceptance_criteria>
  <reversibility rating="one-way">A weakened anti-impersonation/non-disclosure clause that ships unnoticed could leak buyer identity or platform identity to a supplier in a live outreach email before anyone notices — this is why Task 1's checkpoint:decision precedes this edit and Task 3's checkpoint:human-verify follows it, per D-04.</reversibility>
  <done>lib/agents.ts and tests/prompt-injection-defense.test.ts are fully renamed; the chosen verification depth from Task 1 has been executed and its result recorded for Task 3's review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human confirms guard behavior unchanged</name>
  <what-built>lib/agents.ts's ~17 brand-string occurrences renamed one at a time, including the two security-critical identityRules non-disclosure clauses and the INJECTION_DEFENSE anti-impersonation clause's explanatory comment; tests/prompt-injection-defense.test.ts's 2 incidental comment mentions updated.</what-built>
  <how-to-verify>
    Review `git diff lib/agents.ts` and confirm: (1) every changed line only swaps
    "SourceIQ"/"sourceiq" for "SourceGPT"/"sourcegpt", (2) the identityRules
    conditional's disclosed/anonymous branching logic is byte-identical apart from the
    brand word, (3) `npm test -- tests/prompt-injection-defense.test.ts` passes all 7
    assertions. If static-plus-live-smoke-test was chosen in Task 1, also review the two
    manually-generated outreach email samples from Task 2 and confirm neither discloses
    buyer identity (anonymous case) or the platform name (disclosed case).
  </how-to-verify>
  <resume-signal>Type "approved" once the guard's behavior is confirmed unchanged, or describe the specific issue found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lib/agents.ts prompt text → live outreach email sent to a real, external supplier | The identityRules clause is the only guard preventing buyer/platform identity leakage in anonymous outreach |
| lib/agents.ts prompt text → web_search / supplier-provided content ingestion | INJECTION_DEFENSE is the only guard against prompt injection from untrusted scraped/searched content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure | lib/agents.ts identityRules (anonymous-outreach branch) | critical | mitigate | Manual one-at-a-time edit (never scripted); checkpoint:decision before the edit selects verification depth; checkpoint:human-verify after the edit confirms behavior unchanged, per BRAND-03/D-04 |
| T-01-02-02 | Spoofing | lib/agents.ts INJECTION_DEFENSE anti-impersonation clause | high | mitigate | Same manual-edit-plus-checkpoint process; reversibility rated one-way and explicitly flagged so the edit is never treated as routine |
| T-01-02-03 | Tampering | tests/prompt-injection-defense.test.ts (edited alongside source) | low | mitigate | Read-through diff review confirms only comment lines 7/53 change; no assertion or functionBody() helper logic is touched |

</threat_model>

<verification>
1. `git diff lib/agents.ts` — only brand-word substitutions, no structural/logic changes.
2. `npm test -- tests/prompt-injection-defense.test.ts` passes all 7 assertions.
3. Human checkpoint confirms guard behavior unchanged (static read-through or live smoke test, per Task 1's decision).
</verification>

<success_criteria>
lib/agents.ts and tests/prompt-injection-defense.test.ts are renamed to "SourceGPT"
with zero semantic drift in the INJECTION_DEFENSE and identityRules guard clauses, and
a human has explicitly confirmed the guard's behavior — not just its text — is
unchanged.
</success_criteria>

<output>
Create `.planning/phases/01-rename-brand-migration/01-02-SUMMARY.md` when done
</output>
