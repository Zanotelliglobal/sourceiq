---
phase: 01-rename-brand-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/rename-brand.mjs
  - app/**
  - lib/** (excl. lib/agents.ts)
  - components/**
  - tests/** (excl. tests/prompt-injection-defense.test.ts)
  - lib/i18n/*.ts
  - docs/*.md (excl. docs/change-request-backlog.md)
  - docs/brand/*
  - package.json
  - package-lock.json
  - start.sh
  - README.md
  - design-system/MASTER.md
autonomous: true
requirements: [BRAND-01, BRAND-02]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "package.json's name field reads 'sourcegpt' and package-lock.json is regenerated to match (not hand-edited)"
    - "Every UI copy string, page title, i18n dictionary key/value pair (de/es/fr/it), and contact-email domain in app/, lib/ (excl. agents.ts), components/, tests/ (excl. prompt-injection-defense.test.ts), and docs/ (excl. change-request-backlog.md) reads 'SourceGPT'/'sourcegpt', never 'SourceIQ'/'sourceiq'"
    - "The 6 docs/brand/ asset filenames and the 2 SVGs' embedded text read 'sourcegpt'/'SourceGPT'; the 4 raster (.png/.jpg) files' pixel content is left completely untouched"
  artifacts:
    - scripts/rename-brand.mjs
    - lib/i18n/config.ts (STORAGE_KEY changed to "sourcegpt.lang")
    - docs/brand/sourcegpt-mark-square.svg, docs/brand/sourcegpt-wordmark.svg (renamed from sourceiq- equivalents)
    - docs/competitive-comparison-sourcegpt-vs-sourceready.md (renamed)
    - package-lock.json (regenerated)
  key_links:
    - "i18n dictionary keys in lib/i18n/de.ts|es.ts|fr.ts|it.ts stay byte-identical to the English literal keys used at t() call sites across app/ and components/ — both sides renamed atomically in the same script pass, or lookups silently fail"
    - "package.json's name field flows into package-lock.json via `npm install`, never hand-edited directly"
---

<objective>
Build and run a scripted, case-aware bulk rename (SourceIQ → SourceGPT) across every
file in this repo where a mechanical find/replace is safe, per CONTEXT.md D-01/D-02/D-03.
This excludes the handful of files requiring manual, narrative-aware treatment
(lib/agents.ts, tests/prompt-injection-defense.test.ts, both legal pages,
docs/change-request-backlog.md, .claude/CLAUDE.md — all handled in later plans) and the
explicitly out-of-scope directories/files (the two `-autoresearch` history directories,
`.planning/`, the `sourceiq.db*` SQLite files, and the unrelated `.claude/worktrees/`
tooling artifact directory).

Purpose: Land the bulk of the ~64+ file rename in one reviewable, reversible, scripted
pass so every subsequent plan in this phase (and every later phase) builds under the
final "SourceGPT" name instead of renaming twice.
Output: `scripts/rename-brand.mjs` (disposable tooling, deleted in the phase-closing
plan), every in-scope file renamed/updated, `package-lock.json` regenerated, full
verification suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-rename-brand-migration/01-CONTEXT.md
@.planning/phases/01-rename-brand-migration/01-PATTERNS.md
@scripts/check-audit.mjs
@package.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Build scripts/rename-brand.mjs and prove it end-to-end on the full include scope via --dry-run</name>
  <files>scripts/rename-brand.mjs</files>
  <read_first>scripts/check-audit.mjs (exact conventions to mirror), package.json (script pattern), lib/i18n/config.ts (STORAGE_KEY constant), docs/brand/ directory listing</read_first>
  <action>
    Create scripts/rename-brand.mjs following scripts/check-audit.mjs's exact conventions:
    zero external dependencies, only node: builtins (fs, path, url), a banner comment at
    the top explaining purpose, small single-purpose functions, the
    path.dirname(fileURLToPath(import.meta.url)) idiom for resolving the repo root, and a
    non-zero process.exit(1) on failure. Do not import via the @/* alias — this script
    runs standalone via `node`, outside the Next.js/TS build pipeline. Do not add this
    script to package.json's scripts block (unlike check-audit.mjs, this is disposable
    one-time tooling).

    The script performs a case-aware string replace across three casing variants —
    SourceIQ to SourceGPT, sourceiq to sourcegpt, SOURCEIQ to SOURCEGPT — over an
    explicit include list: app/, lib/ (every file except agents.ts), components/,
    tests/ (every file except prompt-injection-defense.test.ts), lib/i18n/*.ts,
    docs/*.md (every file except change-request-backlog.md), package.json, start.sh,
    README.md, design-system/MASTER.md. The i18n locale files (de.ts/es.ts/fr.ts/it.ts)
    use the literal English string as the dictionary key and a translated string as the
    value — both key and value must be renamed together in the same pass so keys stay
    byte-identical to the t() call-site literals used across app/ and components/.
    lib/i18n/config.ts's STORAGE_KEY constant changes from "sourceiq.lang" to
    "sourcegpt.lang" as part of this same pass (a one-time, non-destructive side effect:
    a returning user's saved language preference resets to the app default once).

    The script must also handle these file-level renames via fs.renameSync (all six are
    untracked by git, confirmed via `git ls-files -- docs/brand/`, so no `git mv` is
    needed): docs/brand/sourceiq-mark-square.svg/.png/.jpg and
    docs/brand/sourceiq-wordmark.svg/.png/.jpg each become their sourcegpt- equivalent.
    Swap the literal brand-word text content (the <title> tag and the text/tspan
    elements) inside only the two renamed .svg files. Leave the four renamed raster
    .png/.jpg files' binary pixel content completely untouched — only their filenames
    change. Also rename
    docs/competitive-comparison-sourceiq-vs-sourceready.md to
    docs/competitive-comparison-sourcegpt-vs-sourceready.md via fs.renameSync before
    running the content-level text replace on it (its content is safe for the mechanical
    swap — it is a competitive comparison of "SourceIQ" vs a "SourceReady" competitor,
    with no narrative-rename sentence to preserve, confirmed by reading it for
    "renam"/"SupplyAI"/external-domain mentions).

    The script must hard-exclude (never touch, never recurse into) these paths — do not
    even walk their directory trees: sourceiq-ux-autoresearch/,
    procurement-app-autoresearch/, .planning/, sourceiq.db, sourceiq.db-shm,
    sourceiq.db-wal, .claude/worktrees/, node_modules/, .next/, .git/. Also exclude
    from this script's scope (handled by manual passes in later plans, not this script):
    package-lock.json (regenerated in Task 2 below, never text-swapped directly),
    lib/agents.ts, tests/prompt-injection-defense.test.ts, app/legal/privacy/page.tsx,
    app/legal/terms/page.tsx, docs/change-request-backlog.md, and .claude/CLAUDE.md.

    Support two modes: `--dry-run` (default; walks every included file, counts
    case-variant matches per file, prints a per-file summary table including the planned
    file renames, writes nothing) and `--write` (applies both the content replacements
    and the file renames). Run `node scripts/rename-brand.mjs --dry-run` and manually
    inspect the full per-file report end-to-end before writing anything — this proves
    the include/exclude glob logic, the case-aware regex, and the filename-rename logic
    are all correct in one pass, on the complete scope, before any file is mutated.
  </action>
  <verify>
    <automated>node scripts/rename-brand.mjs --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - scripts/rename-brand.mjs exists, has zero external dependencies, and exits 0 on --dry-run
    - The dry-run report lists every file in the include scope with a nonzero match count where "sourceiq" (any case) appears, and lists the 7 planned file renames (6 docs/brand/ assets + 1 competitive-comparison doc)
    - The dry-run report shows zero planned changes to any excluded path (lib/agents.ts, tests/prompt-injection-defense.test.ts, legal pages, docs/change-request-backlog.md, .claude/CLAUDE.md, package-lock.json, the two -autoresearch dirs, .planning/, sourceiq.db*, .claude/worktrees/)
  </acceptance_criteria>
  <reversibility rating="reversible">A scripted case-aware replace re-run in the reverse direction (or a `git checkout` restore, since all touched files are git-tracked except the docs/brand/ assets and the competitive-comparison doc filename) fully undoes this; nothing here is a published external contract, per D-01/D-03.</reversibility>
  <done>scripts/rename-brand.mjs exists and its --dry-run output has been reviewed end-to-end against the include/exclude scope above.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the rename, regenerate the lockfile, and run the full verification suite</name>
  <files>package-lock.json (regenerated), all files in the include scope from Task 1</files>
  <read_first>scripts/rename-brand.mjs (the reviewed dry-run output from Task 1)</read_first>
  <action>
    Run `node scripts/rename-brand.mjs --write` to apply the reviewed changes across the
    full include scope from Task 1. Then run `npm install` to regenerate
    package-lock.json from the now-updated package.json (name field is "sourcegpt") —
    do not hand-edit package-lock.json's internal name fields directly; npm's lockfile
    is derived from package.json, not maintained by hand. No new dependencies are added
    or upgraded by this install — it is a lockfile-metadata regeneration only, so the
    Package Legitimacy Audit gate does not apply here (no new package.json dependency
    entries are introduced). Then run the project's standing full verification command,
    in order: `npm run typecheck`, `npm run lint`, `npm test`, `npm run build` (per D-08
    and the project's existing convention), confirming each exits 0. Finally run an
    interim, scope-limited grep sweep restricted to exactly the files this script
    touched, confirming zero remaining case-insensitive "sourceiq" hits in that scope —
    this is deliberately narrower than the phase-final BRAND-05 sweep (Plan 01-04),
    since lib/agents.ts, the two legal pages, and the two narrative docs are
    intentionally still untouched at this point and are expected to still show hits
    until the later plans land.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/rename-brand.mjs --write` completes with exit code 0
    - package-lock.json's two "name" fields (lines ~2, ~8) read "sourcegpt", regenerated by `npm install`, not hand-edited
    - `npm run typecheck && npm run lint && npm test && npm run build` all exit 0
    - A grep restricted to the include-scope paths (app/, lib/ excl. agents.ts, components/, tests/ excl. prompt-injection-defense.test.ts, lib/i18n/, docs/ excl. change-request-backlog.md, package.json, start.sh, README.md, design-system/MASTER.md, docs/brand/) for case-insensitive "sourceiq" returns zero lines
  </acceptance_criteria>
  <done>Bulk rename applied and committed to the working tree, package-lock.json regenerated with the new name, and the full verification suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rename-brand.mjs execution → repo working tree | A locally-run Node script with unrestricted filesystem write access to the entire working tree |
| npm install → package-lock.json | Regenerating the lockfile pulls fresh metadata from the npm registry for already-declared dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01-01 | Tampering | scripts/rename-brand.mjs's file walk | medium | mitigate | Explicit hardcoded include/exclude allowlist (not a broad recursive glob); --dry-run reviewed end-to-end across the full scope before any --write; excluded paths (agents.ts, legal pages, autoresearch dirs, .planning/, sourceiq.db*, .claude/worktrees/) are never walked, so no accidental over-broad match is possible |
| T-01-01-02 | Denial of Service | scripts/rename-brand.mjs write phase | low | mitigate | Dry-run-first workflow lets failures surface before any file is mutated; each file write is independent (a crash mid-run leaves only already-processed files changed, recoverable via `git diff`/`git checkout`) |
| T-01-01-SC | Tampering | `npm install` (lockfile regeneration) | low | accept | No new package.json dependency entries are introduced this task — same audited dependency set as before; Package Legitimacy Audit gate is scoped to new-package-install tasks, which this is not |

</threat_model>

<verification>
1. `node scripts/rename-brand.mjs --dry-run` runs clean and its report matches the documented include/exclude scope.
2. `node scripts/rename-brand.mjs --write` applies changes; `git status` shows only in-scope files modified/renamed.
3. `npm install` regenerates package-lock.json with "sourcegpt" as the name.
4. `npm run typecheck && npm run lint && npm test && npm run build` all pass.
5. Scoped grep (include-scope paths only) for case-insensitive "sourceiq" returns zero lines.
</verification>

<success_criteria>
scripts/rename-brand.mjs exists and has been run in --write mode across the full
documented include scope; package.json and package-lock.json both read "sourcegpt";
the 6 docs/brand/ assets and the competitive-comparison doc are renamed; the full
verification suite is green; lib/agents.ts, tests/prompt-injection-defense.test.ts,
both legal pages, docs/change-request-backlog.md, and .claude/CLAUDE.md remain
untouched, ready for the manual passes in Plans 01-02 and 01-03.
</success_criteria>

<output>
Create `.planning/phases/01-rename-brand-migration/01-01-SUMMARY.md` when done
</output>
