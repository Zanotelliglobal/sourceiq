---
phase: 01-rename-brand-migration
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/legal/privacy/page.tsx
  - app/legal/terms/page.tsx
  - docs/change-request-backlog.md
  - .claude/CLAUDE.md
autonomous: true
requirements: [BRAND-01]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "app/legal/privacy/page.tsx and app/legal/terms/page.tsx read 'SourceGPT' throughout with no corrupted sentence structure or capitalization, per BRAND-01 and D-07"
    - "docs/change-request-backlog.md's rename-narrative sentence reads as a coherent, grammatically-correct historical record post-edit, and its external 'sourceiq.cloud' competitor-site reference is untouched"
    - ".claude/CLAUDE.md's Project section reads 'SourceGPT' without a stale '(renaming to...)' qualifier, and its now-satisfied rename-sequencing constraint is updated to reflect completion"
  artifacts:
    - app/legal/privacy/page.tsx
    - app/legal/terms/page.tsx
    - docs/change-request-backlog.md
    - .claude/CLAUDE.md
  key_links:
    - "docs/change-request-backlog.md's item #1 open-questions text and the actual rename mechanics executed in Plans 01-01/01-02 remain mutually consistent after the narrative edit — the doc should read as an accurate historical record of what happened, not a live to-do"
---

<objective>
Manually rename the two legal pages and two narrative documents that the bulk script
from Plan 01-01 deliberately excluded, because each requires reading the surrounding
prose (not a blind substitution) to avoid corrupting legal meaning or historical
narrative accuracy.

Purpose: `app/legal/privacy/page.tsx` and `app/legal/terms/page.tsx` are user-facing
legal text where a mechanical replace could silently break a sentence's legal meaning
(e.g. a mid-clause substitution shifting a defined term's boundary), per D-07.
`docs/change-request-backlog.md` contains both the exact narrative sentence describing
this rename ("The startup is renaming from SourceIQ to SourceGPT...") — which must be
rephrased to reflect completion, not blindly substituted into a self-contradictory
sentence — and a reference to the external competitor site `sourceiq.cloud`, which must
NOT be renamed since it is not this product's brand. `.claude/CLAUDE.md`'s project
description carries the same "(renaming to SourceGPT)" narrative qualifier that is now
stale once this phase completes.
Output: Both legal pages and both narrative documents renamed with meaning preserved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-rename-brand-migration/01-CONTEXT.md
@app/legal/privacy/page.tsx
@app/legal/terms/page.tsx
@docs/change-request-backlog.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Manually rename app/legal/privacy/page.tsx and app/legal/terms/page.tsx</name>
  <files>app/legal/privacy/page.tsx, app/legal/terms/page.tsx</files>
  <read_first>app/legal/privacy/page.tsx (all 160 lines), app/legal/terms/page.tsx (all 141 lines)</read_first>
  <action>
    Read both legal pages in full. Swap every literal "SourceIQ"/"sourceiq" occurrence
    for "SourceGPT"/"sourcegpt" while reading each surrounding sentence to confirm the
    substitution does not corrupt legal meaning — check specifically for: mid-sentence
    capitalization that a mechanical script would get wrong (e.g. a sentence-initial
    "SourceIQ" vs. a mid-sentence reference), any defined-term usage where "SourceIQ"
    is introduced as a defined term (e.g. "SourceIQ ('the Service')") whose surrounding
    parenthetical or quotation must stay grammatically intact after the swap, and any
    company/entity-name clause that might be legally distinct from the product brand
    name (do not alter a legal entity name if one is present — only the product brand
    word "SourceIQ" itself). Preserve all other text, structure, and formatting exactly.
  </action>
  <verify>
    <automated>grep -ic sourceiq app/legal/privacy/page.tsx app/legal/terms/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Every "SourceIQ"/"sourceiq" occurrence in both files is replaced with "SourceGPT"/"sourcegpt"
    - No sentence's grammar, capitalization, or legal defined-term structure is altered beyond the brand-word swap
    - `grep -ic sourceiq app/legal/privacy/page.tsx app/legal/terms/page.tsx` returns 0 for both files
  </acceptance_criteria>
  <done>Both legal pages read "SourceGPT" throughout with legal meaning intact, confirmed by a full read-through.</done>
</task>

<task type="auto">
  <name>Task 2: Manually rename docs/change-request-backlog.md's narrative sentence and .claude/CLAUDE.md's project description, preserving the external sourceiq.cloud reference</name>
  <files>docs/change-request-backlog.md, .claude/CLAUDE.md</files>
  <read_first>docs/change-request-backlog.md lines 1-64 (backlog item #1: full rename scope, grep counts, open questions, and the narrative sentence), .claude/CLAUDE.md "## Project" section and its "Rename sequencing" constraint bullet</read_first>
  <action>
    In docs/change-request-backlog.md, locate the narrative sentence describing this
    rename ("The startup is renaming from SourceIQ to SourceGPT, corrected from an
    earlier 'SupplyAI' placeholder..." around line 16) and rewrite it in past tense to
    reflect that the rename is now complete (e.g., "The startup renamed from SourceIQ
    to SourceGPT..."), rather than blindly substituting both brand words into a sentence
    that would otherwise read as self-contradictory ("renaming from SourceGPT to
    SourceGPT"). Elsewhere in this file, rename every other literal "SourceIQ"/"sourceiq"
    occurrence that refers to this product to "SourceGPT"/"sourcegpt". Do NOT rename any
    occurrence of `sourceiq.cloud` — confirmed (lines 3-4 of this file) to be an
    external, unrelated reference/competitor site used for design inspiration, not this
    product's own domain.

    In .claude/CLAUDE.md's "## Project" section, change the heading line
    "**SourceIQ (renaming to SourceGPT)**" to "**SourceGPT**", and rewrite the sentence
    "The product is mid-rebrand to SourceGPT." to reflect that the rebrand is complete
    (e.g., remove that sentence or replace it with a note that the product completed its
    rename from SourceIQ to SourceGPT). In the "### Constraints" section, update the
    "Rename sequencing" bullet ("land the SourceIQ→SourceGPT rename (backlog #1) first,
    before other backlog items...") to reflect that this sequencing requirement is now
    satisfied, since this phase's entire purpose is to make this constraint true — do
    not delete the bullet outright if it documents a decision rationale still relevant
    to why later phases were sequenced this way; instead reword it to past tense
    (e.g., "The SourceIQ→SourceGPT rename (backlog #1) was landed first, before other
    backlog items, so new copy/code lands under the final name.").
  </action>
  <verify>
    <automated>grep -in "sourceiq" docs/change-request-backlog.md .claude/CLAUDE.md | grep -v "sourceiq.cloud" | grep -vi "renamed from sourceiq"</automated>
  </verify>
  <acceptance_criteria>
    - docs/change-request-backlog.md's narrative sentence reads in past tense as a coherent, non-self-contradictory historical record
    - docs/change-request-backlog.md's `sourceiq.cloud` external-domain reference is byte-for-byte unchanged
    - .claude/CLAUDE.md's "## Project" heading reads "**SourceGPT**" with no "(renaming to...)" qualifier, and the "Rename sequencing" constraint bullet is reworded to past tense
    - The verify grep (excluding the two deliberately-preserved patterns) returns no lines
  </acceptance_criteria>
  <done>docs/change-request-backlog.md and .claude/CLAUDE.md both read "SourceGPT" for every own-product mention, the sourceiq.cloud external reference is preserved exactly, and both narrative sentences read as coherent completed-tense prose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app/legal/{privacy,terms}/page.tsx → end user | User-facing legal text; a corrupted substitution could change a legally-binding sentence's meaning |
| docs/change-request-backlog.md → internal historical record | Not user-facing, but a corrupted narrative could misrepresent what actually happened during this rename, or misattribute an external competitor's domain as this product's own |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03-01 | Tampering | app/legal/privacy/page.tsx, app/legal/terms/page.tsx | medium | mitigate | Full manual read-through (not a blind script) per D-07; verify checks both files for zero remaining case-insensitive hits after the manual pass |
| T-01-03-02 | Tampering | docs/change-request-backlog.md narrative sentence | low | mitigate | Manual past-tense rewrite instead of blind substitution, avoiding a self-contradictory historical record; explicit exclusion of the external sourceiq.cloud reference from any substitution |

</threat_model>

<verification>
1. Both legal pages read "SourceGPT" throughout, `grep -ic sourceiq` returns 0 for both.
2. docs/change-request-backlog.md's narrative sentence is coherent and past-tense; `sourceiq.cloud` is untouched.
3. .claude/CLAUDE.md's Project section reads "SourceGPT" with no stale rebrand qualifier.
</verification>

<success_criteria>
Both legal pages and both narrative documents are renamed with their prose meaning
intact, and the one deliberately-preserved external domain reference
(`sourceiq.cloud`) is confirmed untouched.
</success_criteria>

<output>
Create `.planning/phases/01-rename-brand-migration/01-03-SUMMARY.md` when done
</output>
