---
phase: 4
slug: supplier-star-ratings
status: draft
shadcn_initialized: false
preset: none
created: 2026-08-21
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**Scope:** One additive control — a 5-star rating row inside `DetailPanel`
(`app/events/[id]/page.tsx`), rendered next to the existing thumbs-up/down
`feedback_signal` toggle. No new pages, routes, or layouts. All values below
are pulled from — and must not contradict — the project's canonical design
system at `design-system/MASTER.md`, `app/globals.css`, and the exact
existing DetailPanel section (`app/events/[id]/page.tsx:389-431`).

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — bespoke Tailwind design system (no shadcn/Radix/Base UI anywhere in this codebase) |
| Preset | not applicable |
| Component library | none — hand-rolled Tailwind utility classes + `@layer components` primitives (`.btn-primary`, `.card`, `.badge-*` in `app/globals.css`) |
| Icon library | `lucide-react` (already a project dependency, already imported in this exact file — `Star`, `ThumbsUp`, `ThumbsDown` at `app/events/[id]/page.tsx:6-10`) |
| Font | Plus Jakarta Sans (`--font-sans`), tabular-nums (`font-variant-numeric`) for any numeric display — per `design-system/MASTER.md` "Typography — dual track" |

**shadcn gate decision:** `components.json` not found (confirmed via `ls`). This
is NOT a greenfield project — it is an existing, revenue-generating production
app with a fully documented, actively-used design system
(`design-system/MASTER.md`, explicitly labeled "Source of Truth", last
reconciled for this exact procurement/enterprise product). Introducing shadcn
now would fragment 4 completed phases of hand-rolled Tailwind components
(`.btn-primary`, `.card`, `.badge-stage-*`, etc.) rather than extend them.
Decision: **do not initialize shadcn**. This one-line, single-component
addition follows the existing bespoke system instead. Registry safety gate:
not applicable (no registry involved).

---

## Spacing Scale

Declared values (must be multiples of 4), per `design-system/MASTER.md`
"8px spacing rhythm" (dashboard density):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Gap between the 5 star icon-buttons (`gap-1`, matches existing thumbs-button gap at `page.tsx:396`) |
| sm | 8px | Gap between the "Rating" section label and the star row (`mb-2`, matches existing "AI Assessment" label pattern at `page.tsx:393`) |
| md | 16px | Vertical gap between this new Rating block and the adjacent AI Assessment/thumbs block in `DetailPanel`'s content stack |
| lg | 24px | Not used by this phase's UI |
| xl | 32px | Not used by this phase's UI |
| 2xl | 48px | Not used by this phase's UI |
| 3xl | 64px | Not used by this phase's UI |

Exceptions (pre-existing codebase conventions, not newly introduced by this
phase — kept for visual consistency with the immediately adjacent thumbs
control):
- `p-1` (4px) button padding per star icon-button, matching the existing
  thumbs button padding exactly (`page.tsx:402,415`).
- `mt-1` (4px) for optional helper/meta text spacing below the star row, if
  a helper line is ever added. This supersedes the previous draft's `mt-1.5`
  (6px), which violated the multiple-of-4 spacing rule — no half-step
  exception is needed or permitted here; `mt-1` (4px, the `xs` token already
  declared above) satisfies both the visual-consistency goal and the hard
  numeric rule.
- Icon size: `w-3.5 h-3.5` (14px) per star icon — this is the **authoritative
  value** for the executor, matching the existing thumbs icon size exactly
  (`page.tsx:408,421`) and deliberately smaller than the unrelated shortlist
  `Star` icon (`w-4 h-4` / `w-2.5 h-2.5` / `w-3 h-3` at three other call
  sites) to reinforce D-05's required visual distinction. Note:
  04-RESEARCH.md's illustrative code example uses `w-4 h-4` (16px) — that
  example predates this sizing decision and is superseded here; the planner
  and executor must implement `w-3.5 h-3.5` (14px), not the research
  snippet's 16px.

---

## Typography

Per `design-system/MASTER.md` type scale (`12 · 14 · 16 · 18 · 24 · 32 · 48`,
body 16px min) — this phase's UI is narrow (an inline control, not a page),
so only the label role is newly introduced; it reuses the exact existing
DetailPanel section-header typography verbatim for consistency within the
same panel (a declared, precedented exception to the 12px floor, since it
matches the sibling "AI Assessment" / "Key Export Markets" headers character
for character):

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Label ("Rating" section header) | 10px (`text-[10px]`) | 700 bold, uppercase, `tracking-widest` | 1.2 |
| Meta/helper (if a hint line is ever added below the stars) | 12px (`text-xs`) | 400 regular | 1.5 |
| Body | n/a — not used by this control | n/a | n/a |
| Heading | n/a — not used by this control | n/a | n/a |
| Display | n/a — not used by this control | n/a | n/a |

Weights used: 700 (bold, label only) + 400 (regular, meta only) — exactly 2,
matching contract limit.

---

## Color

Per `design-system/MASTER.md` "Trust Blue + Signal Amber" — amber is
explicitly reserved for **the single primary CTA per screen** and must
**never compete with status colors**. This is directly load-bearing for D-05:
the unrelated "Add to Short List" `Star` icon in this exact file already uses
`.btn-primary`/amber-adjacent shortlist styling (`page.tsx:567,902,2496`,
`.badge-stage-shortlisted` = amber). The new rating control must NOT reuse
amber, or it collides with that existing CTA's visual language.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#FFFFFF` (`--color-surface`) / `#F8FAFC` (`--color-background`) | `DetailPanel` card background and page canvas — unchanged, inherited |
| Secondary (30%) | `#64748B` (`--color-muted-fg`, slate-500) / `#E2E8F0` (`--color-border`, slate-200) | "Rating" label text color, inactive/unfilled star icon color (`text-slate-300` fill=none), hover-state border on star buttons |
| Accent (10%) | `#2563EB` (`--color-primary`, blue-600) | Filled/active star icons ONLY (`fill="currentColor"` with `text-blue-600`) — see rationale below |
| Destructive | `#DC2626` (`--color-destructive`) | Not applicable to this phase — no destructive action exists (rating clear-to-null is benign/reversible, D-06/D-07) |

Accent reserved for: **filled star icons in the new rating control, and
nothing else in this phase.** Explicitly NOT amber (`#D97706`) — amber stays
reserved for the pre-existing "Add to Short List" CTA per
`design-system/MASTER.md`'s "amber = single primary CTA... never competes
with status colors" rule. Trust Blue (`#2563EB`) is used instead because it
is the app's dominant non-CTA interactive-state color, and is already
visually distinct from both amber (shortlist) and emerald/red (the adjacent
thumbs feedback control uses `emerald-600`/`red-600` for its active states,
`page.tsx:404,417`) — three co-located controls (thumbs, stars, shortlist),
three distinct color families, zero collisions.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | Not applicable — no page-level CTA in this phase; the interaction is direct manipulation (click a star), not a button-triggered submission |
| Section label | `"Rating"` — `text-[10px] font-bold uppercase tracking-widest`, matches sibling "AI Assessment" header exactly, routed through `useT()` (`t("Rating")`) per this codebase's i18n convention (de/es/fr/it locales) |
| Per-star aria-label | `t("Rate {n} stars", { n })` for n = 1–5 — exact pattern from 04-RESEARCH.md's recommended shape; required since the control has no visible numeric text, only icons |
| Empty state (unrated, `identity_id` present but `rating` is null) | No copy needed — all 5 stars render unfilled (`fill="none"`, slate-300); the "Rating" label alone communicates the control's purpose. Do not add placeholder text like "Not yet rated" — keeps parity with the icon-only thumbs control's zero-state (no label either) |
| Hidden state (`identity_id IS NULL`, D-03) | No copy at all — the entire control is omitted from the DOM, not shown disabled/greyed. This is a decision already locked in CONTEXT.md D-03, not a Claude's-Discretion item |
| Error state | `t("Could not save rating. Please try again.")` — exact string, mirrors the existing `setFeedback` error-toast pattern verbatim (04-RESEARCH.md Code Examples, `pushToast("error", ...)`) |
| Destructive confirmation | Not applicable — no destructive action in this phase. Re-clicking an active star clears the rating with NO confirmation step (D-06/D-07 lock this as a low-friction, freely-reversible toggle, identical to the existing thumbs toggle-to-clear pattern) |

---

## UI Considerations

Generated via `ui-consideration-probe.cjs` (deterministic engine, ui-phase.md Step 9.5)
against 2 authored elements — `E1-star-row` (kind: `interactive-control`) and
`E2-rating-label` (kind: `static-content`) — plus 3 additional open,
domain-specific UX rows the closed 8-category taxonomy doesn't reach (prose-owned,
per `references/domain-probes.md`'s content/robustness-vs-open-UX split).

Engine coverage: 5/5 applicable considerations closed (2 resolved-explicit,
1 resolved-backstop, 2 dismissed, 0 unresolved). Plus 3 prose-owned rows, all
covered. **Total: 8/8 covered, 0 unresolved.**

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| populated | star row (rating = 1–5) | ✅ covered (prose-owned) | Stars render filled (blue-600, `fill="currentColor"`) up to the current rating value; unfilled stars render `fill="none"`, slate-300 — per D-05 and 04-RESEARCH.md's code pattern |
| empty/zero | star row (rating = null, `identity_id` present) | ✅ covered (prose-owned) | All 5 stars render unfilled; "Rating" label still visible — see Copywriting Contract "Empty state" row |
| zero-one-many | control visibility (`identity_id` null vs. not) | ✅ covered (prose-owned) | D-03 (locked): rows with `identity_id IS NULL` (all pre-Phase-4 rows) simply omit the control entirely — no disabled/tooltip state |
| loading | `E1-star-row` (interactive-control) | ✅ resolved — explicit | No loading state exists: `identity_id` is written synchronously at the same INSERT/UPDATE as the `suppliers` row (D-03), so there is no async resolution window. Until then, the control is entirely absent from the DOM (same treatment as the hidden/legacy-row case) |
| error | `E1-star-row` (interactive-control) | ✅ resolved — explicit | Optimistic update with revert-on-failure; on failure show toast `t("Could not save rating. Please try again.")`, mirroring the existing `setFeedback` error-toast pattern verbatim |
| overflow | `E2-rating-label` (static-content) | ✅ resolved — backstop | Label reuses the exact sibling "AI Assessment"/"Key Export Markets" header pattern already proven at this panel width across all 4 locales (de/es/fr/it); no new overflow risk beyond that existing, already-shipped pattern |
| long-text | `E1-star-row` + `E2-rating-label` | ⛔ dismissed | Fixed 5-icon-only control with no dynamic/variable-length text rendered inside it (aria-labels aren't visually rendered); "Rating" is a fixed short single-word label whose translated equivalents (e.g. "Bewertung", "Calificación") are similarly short — long-text truncation/wrapping doesn't apply to either element |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| shadcn official | none — shadcn not initialized for this phase | not applicable |
| third-party | none | not applicable |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
