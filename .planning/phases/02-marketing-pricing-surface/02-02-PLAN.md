---
phase: 02-marketing-pricing-surface
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/LandingContent.tsx
  - public/hero-placeholder.svg
autonomous: true
requirements: [PRICE-01, PRICE-03, MKT-01, MKT-04, MKT-05]

estimate:
  tokens: 55000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "As a buyer visiting the public landing page, I want to see 4 tier cards (Basic, Growth, Premium, Enterprise) with real USD pricing sourced from lib/plans.ts, a 6-tile feature grid that matches actual shipped functionality, a closing CTA banner, and a media slot in the hero, so that I understand the product's price and capabilities before signing up (PRICE-01, PRICE-03, MKT-01, MKT-04, MKT-05)"
    - "The pricing card array in components/LandingContent.tsx is rendered by mapping over TIERS.filter(t => t.key !== 'free') from lib/plans.ts, NOT by a hardcoded local array — a price change in lib/plans.ts must automatically flow to the landing page (D-04)"
    - "Exactly one pricing card CTA links to /sign-up with a 'Start free trial' label — the Basic-tier card — and it is the only card marketed as a trial entry point (PRICE-03, D-05)"
    - "The Growth-tier card carries the amber 'Most popular' badge (via the `featured: true` flag on the Growth tier in lib/plans.ts) — the sole non-hero amber accent on the landing pricing section (UI-SPEC Color section, accent site #2 of 4)"
    - "The Enterprise-tier card's CTA reads 'Contact sales' and links to mailto:hello@sourcegpt.org (sourced from COMPANY.contactEmail in lib/legal.ts) — never a checkout button, never a $0 price display, never a 'Start free trial' label"
    - "The feature grid renders exactly 6 tiles in this order: Autonomous Sourcing Engine, Compliance & Audit, Workflow Automation, Collaboration Hub, Budget & Spend Intelligence, Supplier Marketplace (repositioned copy) — RFP & Intake Tools and ERP/API Integrations tiles are not present (MKT-04 D-06; count sourced from docs/change-request-backlog.md §6: 2 include-as-is + 3 reword + 1 reposition = 6, per D-06 canonical mapping)"
    - "The Supplier Marketplace tile copy is exactly the finalized backlog wording: 'AI discovers and verifies suppliers live from the web.' (MKT-04 D-06 — locked backlog copy)"
    - "The closing CTA banner (MKT-01, D-10) is added AFTER the existing 'Final CTA' section (currently ~lines 189-209) and BEFORE the former inline footer — a distinct, separate section from the pre-existing Final CTA, with heading 'Don't see a perfect fit?' per UI-SPEC Copywriting Contract"
    - "The Hero product-preview block (currently ~lines 51-92, hardcoded fake supplier rows) is replaced or wrapped so a single <img> element referencing /hero-placeholder.svg is the primary media surface — a real screenshot/video can be swapped in later by replacing the file at public/hero-placeholder.svg with zero code change (MKT-05 D-08)"
    - "public/hero-placeholder.svg exists as a real static asset committed to the repo (not a placeholder reference to a nonexistent file)"
    - "The inline <footer> block currently at lines 212-228 in components/LandingContent.tsx is DELETED — the global SiteFooter.tsx (rendered by app/layout.tsx) is the sole footer on the landing page (RESEARCH Pattern 3 — the pre-existing duplicate-footer bug is fixed by this phase)"
    - { statement: "The pricing card 'featured' rendering visits exactly one tier — the tier where TIERS.find(t => t.featured === true) resolves — matching the sole amber-accent-per-section rule (UI-SPEC Color, adjacency edge lifted per PRICE-04)", verification: backstop }
    - { statement: "For an empty TIERS array or a TIERS with contactSales-only tiers (hypothetical future edge), the pricing section still renders without crash — an empty grid is acceptable but a runtime error is not (empty-input edge lifted per PRICE-04)", verification: backstop }
    - { statement: "Pricing card CTA loading state: the Basic-tier 'Start free trial' link points at /sign-up (a static Next.js route) — no client-side spinner/busy state fires from this landing card because navigation is server-driven; the 'Redirecting…' busy-state copy lives on /billing's Choose-{Tier} buttons (plan 02-01), not on landing card CTAs (UI-SPEC UI Considerations loading — lifted as backstop; landing-card CTAs are hyperlinks, not async initiators, so no loading-state UI is rendered here)", verification: backstop }
    - { statement: "Pricing card CTA error state: the landing-page pricing card CTAs are plain hyperlinks (Link to /sign-up or mailto:hello@sourcegpt.org) — they cannot fail-in-place with an inline error message; the red-600/red-50 inline checkout/redirect failure copy lives on /billing (plan 02-01), not on this landing surface (UI-SPEC UI Considerations error — lifted as backstop; landing-card CTAs have no error surface because they do not initiate checkout)", verification: backstop }
    - { statement: "Feature-grid tile body copy is capped at ~110 characters (2 lines at 16px/1.5 line-height) per UI-SPEC UI Considerations long-text row — no tile body exceeds this cap; every shipped tile body copy from the UI-SPEC Copywriting Contract already fits (Autonomous Sourcing Engine 110 chars, Compliance & Audit 108 chars, Workflow Automation 105 chars, Collaboration Hub 89 chars, Budget & Spend Intelligence 98 chars, Supplier Marketplace 51 chars — all under 110)", verification: backstop }
    - "Every new user-facing string added by this plan (feature-grid tile titles/bodies, closing CTA banner heading/body/button, Hero caption) is wrapped in t(useT()) per the useT() i18n pattern used throughout the existing file — no hardcoded English strings outside t() calls (PATTERNS.md i18n rule)"
  prohibitions:
    - "MUST NOT overstate the feature-grid tile copy beyond what SourceGPT actually ships today (MKT-04, transparency): Workflow Automation tile must reflect real background-agent enrichment behavior (lib/task-pool.ts) — not claim a workflow-builder or drag-drop editor that does not exist; Collaboration Hub must reflect real multi-seat orgs (Clerk, seats limit) — not claim real-time co-editing, comments, or chat that do not exist; Budget & Spend Intelligence must reflect real per-event maxEventSpendUsd ceiling (lib/usage.ts) — not claim cross-event analytics dashboards that do not exist. The UI-SPEC Copywriting Contract drafts are the anchor; any deviation must preserve the not-overstating constraint."
    - "MUST NOT import Facebook, Instagram, or LinkedIn (or any social brand icons) from lucide-react anywhere in this file — lucide-react v1.x removed brand icons entirely (Pitfall 3). Social icons are handled in plan 02-03's SocialIcons component; this plan MUST NOT create a competing icon import."
    - "MUST NOT add a second amber-accent CTA to the landing page beyond the four declared sites (Hero CTA, Growth pricing badge+CTA, Final CTA button, Closing CTA banner 'Talk to sales' button) — the UI-SPEC Color contract caps amber at 4 uses; any additional amber-accented element is a contract violation, transparency: it dilutes the primary-action signal."
  artifacts:
    - "components/LandingContent.tsx (modified) — 4-tier pricing card array reading from TIERS.filter, 6-tile feature grid section (new), closing CTA banner section (new), Hero media slot referencing /hero-placeholder.svg, inline <footer> block deleted"
    - "public/hero-placeholder.svg (new) — static SVG asset used by the Hero media slot; a flat-color or product-mock illustration acceptable per D-08"
  key_links:
    - "The pricing card array MUST read TIERS.filter(t => t.key !== 'free') live from lib/plans.ts — if it re-hardcodes prices/limits locally, any future lib/plans.ts change silently drifts (this is exactly the 'don't duplicate the single source of truth' failure mode called out in RESEARCH.md 'Key insight' at the end of Don't Hand-Roll)"
    - "The Growth tier's `featured: true` flag drives the 'Most popular' badge — the badge rendering must key off that flag, NOT off a hardcoded `tier.key === 'growth'` check, so if the featured tier ever moves (e.g. to Premium) it flows through automatically"
    - "The Basic-tier 'Start free trial' CTA links to /sign-up (existing pattern, LandingContent.tsx:177-182), NOT to /api/stripe/checkout — the Stripe-side trial_period_days (from plan 02-01 task 2) fires later when the trialing user goes to /billing and picks Basic. See RESEARCH Open Question 1 (dual trial mechanism)."
    - "The Hero media slot's <img src=\"/hero-placeholder.svg\" /> path resolution depends on public/hero-placeholder.svg existing at commit time — the SVG asset file must be authored in the same plan, not deferred; Next.js's public/ static serving does not synthesize missing assets"

artifacts_produced:
  - "Feature grid section (new — 6 tiles: Autonomous Sourcing Engine, Compliance & Audit, Workflow Automation, Collaboration Hub, Budget & Spend Intelligence, Supplier Marketplace repositioned copy)"
  - "Closing CTA banner section (new — 'Don't see a perfect fit?' heading, 'Talk to sales' button)"
  - "Hero media slot (new — <img src=\"/hero-placeholder.svg\" /> element replacing/wrapping the current hardcoded fake-data preview block)"
  - "public/hero-placeholder.svg (new static asset)"
  - "Restructured pricing card array (4 cards: Basic/Growth/Premium/Enterprise) reading from TIERS.filter(t => t.key !== 'free')"
  - "Deletion of the inline <footer> block at former lines 212-228 (fixes pre-existing duplicate-footer bug)"
---

<objective>
Expand the landing page from the tracer catalog (plan 02-01) into the full public
marketing surface: 4-tier pricing card grid driven live by lib/plans.ts, new 6-tile
"what we do" feature grid, new closing CTA banner, real hero media slot (with a
committed placeholder SVG), and removal of the pre-existing duplicate inline footer so
the global SiteFooter.tsx (extended in plan 02-03) is the sole footer.

Purpose: PRICE-01/PRICE-03 land on the public marketing surface here; MKT-01, MKT-04,
MKT-05 are entirely new landing-page content. This is the primary user-visible payoff
of the phase — the "buyer sees the new pricing/marketing surface" outcome.

Output: components/LandingContent.tsx heavily modified; public/hero-placeholder.svg
newly created. npm run build passes (compile-time catches the lucide-react import
pitfall if it accidentally sneaks in).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-marketing-pricing-surface/02-CONTEXT.md
@.planning/phases/02-marketing-pricing-surface/02-RESEARCH.md
@.planning/phases/02-marketing-pricing-surface/02-PATTERNS.md
@.planning/phases/02-marketing-pricing-surface/02-UI-SPEC.md
@.planning/phases/02-marketing-pricing-surface/02-01-PLAN.md
@components/LandingContent.tsx
@components/LanguageProvider.tsx
@lib/plans.ts
@lib/legal.ts
@app/layout.tsx
@app/page.tsx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static asset serving | public/hero-placeholder.svg is served by Next.js as an untrusted-content-type-safe static image; no user input reaches it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Information Disclosure | Feature-grid tile copy overstating shipped functionality | medium | mitigate | Every reworded tile (Workflow Automation, Collaboration Hub, Budget & Spend Intelligence) must reflect functionality that actually ships today per the UI-SPEC Copywriting Contract drafts — enforced by the prohibition on the tile-copy row and reviewed at the plan 02-04 human checkpoint |
| T-02-04 | Tampering | Broken/missing public/hero-placeholder.svg silently 404s in production | low | accept | Verified at plan 02-04 build + human checkpoint; a missing SVG surfaces as a broken image, not a security event |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restructure the pricing card array to read live from lib/plans.ts and add the Hero media slot with a committed placeholder SVG</name>
  <files>components/LandingContent.tsx, public/hero-placeholder.svg</files>
  <read_first>
    - components/LandingContent.tsx (full file, 231 lines — the file being restructured)
    - lib/plans.ts (post-02-01 state — reads TIERS, Tier.contactSales, Tier.featured, Tier.monthlyUsd, displayPrice, cadenceSuffix)
    - lib/legal.ts (source of COMPANY.contactEmail for the Enterprise mailto CTA)
    - components/LanguageProvider.tsx (lines 44-50 — the useT() fallback-to-English behavior; new copy passes through t() without needing translations to build)
    - .planning/phases/02-marketing-pricing-surface/02-RESEARCH.md sections "Pitfall 2" (contactSales branch), "Pitfall 4" (hero-media-slot requirement), "Environment Availability" (public/ is empty — asset must be created)
    - .planning/phases/02-marketing-pricing-surface/02-PATTERNS.md section for components/LandingContent.tsx (pricing-cards-array-map pattern, i18n rule, hero product-preview conversion)
    - .planning/phases/02-marketing-pricing-surface/02-UI-SPEC.md sections "Color" (amber accent at 4 sites, Growth 'Most popular' badge is site #2), "Copywriting Contract" (Growth CTA "Choose Growth", Premium CTA "Choose Premium", Enterprise CTA "Contact sales" -> mailto, Basic CTA "Start free trial", Hero media caption "Product preview")
  </read_first>
  <behavior>
    - Test 1 (visual/manual, verified in plan 02-04 human checkpoint): The pricing section renders exactly 4 cards — Basic, Growth, Premium, Enterprise — reading tier data from TIERS.filter(t => t.key !== "free") in lib/plans.ts; changing a monthlyUsd value in lib/plans.ts and re-running `npm run build` reflects the new price on the landing page with no other code change
    - Test 2 (visual): The Growth card shows a "Most popular" amber badge because Growth's TIERS entry has `featured: true`; if `featured` moves to another tier in lib/plans.ts, the badge follows without further code change
    - Test 3 (visual): The Basic card's CTA reads "Start free trial" and links to /sign-up; the Growth/Premium cards' CTAs read "Choose {tier.name}" and link to /billing or /sign-up per the existing pattern; the Enterprise card's CTA reads "Contact sales" and links to `mailto:${COMPANY.contactEmail}` — NEVER "Start free trial", NEVER a $-formatted price, NEVER "Free"
    - Test 4 (visual): The Hero section contains an <img> element with src="/hero-placeholder.svg", alt describing a product preview, wrapped in the existing browser-frame card treatment (or replacing it); a caption "Product preview" per UI-SPEC Copywriting Contract is visible; public/hero-placeholder.svg exists as a real committed file
    - Test 5: `npm run build` exits 0 — the compile-time build catches any missed lucide-react brand-icon import (Pitfall 3) and any TypeScript field-rename regression
  </behavior>
  <action>
    Extend components/LandingContent.tsx in place — this task focuses on the pricing card restructure and Hero media slot; Task 2 adds the feature grid and closing CTA; Task 3 deletes the inline footer.
    (a) Import COMPANY from @/lib/legal (add to the existing imports block at the top of the file). Confirm lib/plans.ts exports (TIERS, displayPrice, cadenceSuffix, and the Tier type) are already imported or add them per the existing @/lib/plans import shape.
    (b) In the pricing section (currently lines 143-187), replace the current locally-hardcoded 3-card array (Trial/Growth/Enterprise ~line 152-157) with a map over `TIERS.filter(t => t.key !== "free")`. For each tier render a card that shows:
        - The tier name (t(tier.name))
        - The tier blurb (t(tier.blurb))
        - If `tier.contactSales === true`: no numeric price — instead render a "Contact us" or "Custom pricing" label; if `tier.contactSales !== true`: render `$` + displayPrice(tier, cadence).toLocaleString() using the currently-selected cadence, followed by cadenceSuffix(cadence) — the existing suffix helper from lib/plans.ts
        - The `featured` badge (amber "Most popular" pill, per UI-SPEC) rendered ONLY when tier.featured === true
        - The tier's CTA button: if tier.contactSales === true render "Contact sales" as a link to `mailto:${COMPANY.contactEmail}`; if tier.key === "basic" render "Start free trial" as a Link to /sign-up (existing pattern lines 177-182); else render `t("Choose {plan}", { plan: t(tier.name) })` as a Link to /sign-up
    (c) Preserve the existing cadence-toggle pattern (weekly/monthly/yearly) at the top of the pricing section — the new 4-card grid must respect whichever cadence is currently selected, exactly like the existing 3-card array does today.
    (d) Preserve grid-column count decisions with the extra card: the existing 3-card grid uses `grid-cols-1 md:grid-cols-3`; the new 4-card grid should use `grid-cols-1 md:grid-cols-2 lg:grid-cols-4` (or `md:grid-cols-4` if the copy fits) — pick a Tailwind class combination that renders 4 cards side-by-side on desktop, 2×2 on tablet, stacked on mobile, matching the existing `gap-6` (24px) spacing rhythm from UI-SPEC.
    (e) In the Hero section (currently the "Product preview" block at lines 51-92, hardcoded fake supplier rows), wrap or replace the hardcoded content with an `<img src="/hero-placeholder.svg" alt="Product preview showing SourceGPT's supplier discovery interface" />` element (per Pitfall 4). Keep the existing browser-frame/bordered card treatment as the wrapper — the placeholder image goes inside the frame. Add a small caption below reading `t("Product preview")` per UI-SPEC Copywriting Contract. The existing hardcoded fake stats/supplier rows can either become a graceful fallback (visually hidden or replaced) or be removed entirely — either is acceptable as long as the primary visual anchor is now the <img> element referencing the static asset (D-08).
    (f) Create public/hero-placeholder.svg — a real, committed SVG file. It should be a simple flat-color or minimal product-mock illustration (e.g. a stylized browser-frame with placeholder supplier rows in muted colors), authored inline as SVG (viewBox scale-agnostic, works at any hero-card size). No specific artistic requirement — a "clearly a placeholder" aesthetic is acceptable per D-08. Committed dimensions and format (SVG) are what matter — the file existing is the load-bearing outcome.
    NEVER import Facebook/Instagram/Linkedin from lucide-react in this file (Pitfall 3 — those imports fail at build time; also social icons are plan 02-03's concern). NEVER hardcode monthlyUsd values in this file — read live from lib/plans.ts. NEVER remove the useT()/t() i18n wrapping on any pre-existing string.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20 && test -f public/hero-placeholder.svg && grep -v '^\s*//' components/LandingContent.tsx | grep -cE 'TIERS\.filter' && grep -v '^\s*//' components/LandingContent.tsx | grep -cE '/hero-placeholder\.svg' && grep -v '^\s*//' components/LandingContent.tsx | grep -cE 'contactSales' && test $(grep -v '^\s*//' components/LandingContent.tsx | grep -cE '"lucide-react".*Facebook|"lucide-react".*Instagram|"lucide-react".*Linkedin') -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0 (compile-time catch for Pitfall 3 lucide-react brand-icon import)
    - public/hero-placeholder.svg exists as a file (not a symlink to nothing)
    - Non-comment occurrences of `TIERS.filter` in components/LandingContent.tsx >= 1 (pricing card array reads live from lib/plans.ts)
    - Non-comment occurrences of `/hero-placeholder.svg` in components/LandingContent.tsx === 1 (single <img> reference)
    - Non-comment occurrences of `contactSales` in components/LandingContent.tsx >= 1 (Enterprise-tier discriminator branch)
    - Zero occurrences of Facebook / Instagram / Linkedin imports from "lucide-react" in components/LandingContent.tsx
    - The `featured` prop drives the amber badge — a static grep confirms the badge conditionally renders based on `tier.featured` (or the equivalent) rather than a `tier.key === "growth"` string check
  </acceptance_criteria>
  <done>Landing pricing section reads live from lib/plans.ts, renders 4 tier cards with the correct CTA per contactSales/key discriminator, and the Hero section has a real <img> referencing the newly-committed public/hero-placeholder.svg.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the 6-tile 'what we do' feature grid section and the 'Don't see a perfect fit?' closing CTA banner section</name>
  <files>components/LandingContent.tsx</files>
  <read_first>
    - components/LandingContent.tsx (post-Task-1 state — the file being extended)
    - .planning/phases/02-marketing-pricing-surface/02-UI-SPEC.md sections "Copywriting Contract" (all 6 feature-grid tile rows: Autonomous Sourcing Engine, Compliance & Audit, Workflow Automation, Collaboration Hub, Budget & Spend Intelligence, Supplier Marketplace repositioned; plus Closing CTA banner heading/body/button rows), "Typography" (tile titles are text-base font-semibold — NOT font-bold — new-content weight budget cap of 2 weights), "Spacing" (feature-grid gap-6, section py-24), "Color" (Closing CTA banner "Talk to sales" button is the 4th and last amber-accent site)
    - .planning/phases/02-marketing-pricing-surface/02-CONTEXT.md D-06 (feature-grid section is NEW content, include-as-is + reword + reposition + exclude mapping), D-10 (closing CTA banner is SEPARATE from and AFTER the existing Final CTA section)
    - .planning/phases/02-marketing-pricing-surface/02-PATTERNS.md section for components/LandingContent.tsx ("How it works" section structure at lines 120-141 as the closest analog for the feature-grid card-grid pattern; Final CTA section as the closing-CTA-banner template)
    - lib/legal.ts (source of COMPANY.contactEmail for the "Talk to sales" mailto)
    - docs/change-request-backlog.md §6 (lines 167-194) — MKT-04 feature-grid mapping (authoritative source for include/reword/reposition/exclude decisions)
  </read_first>
  <behavior>
    - Test 1 (visual/manual, verified plan 02-04): A new "what we do" feature grid section renders between the existing "How it works" section (~line 120-141) and the existing "Pricing" section (~line 143), containing exactly 6 tiles in this order: Autonomous Sourcing Engine, Compliance & Audit, Workflow Automation, Collaboration Hub, Budget & Spend Intelligence, Supplier Marketplace (repositioned copy)
    - Test 2 (visual): The Supplier Marketplace tile copy is EXACTLY "AI discovers and verifies suppliers live from the web." — the locked backlog wording (MKT-04 D-06)
    - Test 3 (visual): No RFP & Intake Tools tile is present anywhere on the page; no ERP/API Integrations tile is present anywhere on the page (MKT-04 exclusion — these tiles never existed on this page and must not be introduced)
    - Test 4 (visual): A new "Don't see a perfect fit?" closing CTA banner section renders AFTER the existing "Final CTA" section (currently ~lines 189-209) and BEFORE the former inline footer position (~line 212) — a distinct, separate section
    - Test 5 (visual): The closing CTA banner's "Talk to sales" button is amber-accented (using the existing `.btn-cta` class or equivalent) and links to `mailto:${COMPANY.contactEmail}`; the banner is the 4th and last amber-accent site on the page per UI-SPEC Color
    - Test 6 (visual): Every tile title uses text-base font-semibold (16px/600) per UI-SPEC Typography's new-content weight budget — NOT font-bold; tile body copy is text-base font-normal (16px/400)
  </behavior>
  <action>
    Extend components/LandingContent.tsx in place — this task adds two new sections; the inline-footer deletion is Task 3.
    (a) Add a new feature-grid section between the existing "How it works" section (~lines 120-141) and the "Pricing" section (~line 143). Structure per PATTERNS.md and UI-SPEC:
        - Outer wrapper: `<section className="max-w-screen-xl mx-auto px-4 sm:px-6 py-24">` (matches the 96px `4xl` rhythm already used on every existing section)
        - Heading block: centered H2 (existing pre-existing heading treatment is acceptable for the H2 — it's a pre-existing legacy weight per UI-SPEC), one-line subhead in text-slate-500
        - Grid: `<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">` — 6 tiles fit cleanly as a 3-wide × 2-row grid on desktop, 2-wide × 3-row on tablet, stacked on mobile, matching the 24px `gap-6` rhythm from UI-SPEC Spacing
        - Each tile: card class `.card p-7` (matching the existing "How it works" pattern), containing an icon chip (`w-11 h-11 rounded-xl bg-blue-50 border border-blue-100`) with a lucide-react icon (pick a non-brand lucide icon per tile — Zap, Shield, RefreshCw, Users, DollarSign, Search or similar; do NOT use Facebook/Instagram/Linkedin), then a title (`<h3 className="text-base font-semibold text-slate-900">` — NOT font-bold, per UI-SPEC new-content weight cap), then a body (`<p className="text-base text-slate-500 mt-2 leading-relaxed">`)
        - Tile copy for the 6 tiles (from UI-SPEC Copywriting Contract; feel free to refine wording within the "no overstating" constraint from the prohibitions):
            1. Autonomous Sourcing Engine — "Describe a sourcing need once — AI agents scout, score, and shortlist real suppliers from live web search, end to end."
            2. Compliance & Audit — "Every discovery and outreach action is scoped to your organization and logged, with GDPR-aligned data handling built in."
            3. Workflow Automation — "Background agents automatically enrich and verify suppliers as your pipeline moves — no manual re-checking required."
            4. Collaboration Hub — "Invite your team into a shared organization workspace — everyone sees the same supplier pipeline."
            5. Budget & Spend Intelligence — "Set a spend ceiling per sourcing event and track AI usage cost as it happens — no runaway bills."
            6. Supplier Marketplace (repositioned, LOCKED copy) — "AI discovers and verifies suppliers live from the web." (do NOT paraphrase — this is finalized backlog wording per MKT-04 D-06)
        - Every string wrapped in t(...) per the useT() i18n pattern
        - Tile count: exactly 6 tiles, per docs/change-request-backlog.md §6's authoritative mapping (D-06's cited source of truth): 2 include-as-is (Autonomous Sourcing Engine, Compliance & Audit) + 3 reword (Workflow Automation, Collaboration Hub, Budget & Spend Intelligence) + 1 reposition (Supplier Marketplace) = 6 total. Do NOT ship 5; do NOT reintroduce RFP & Intake Tools or ERP/API Integrations (both excluded per D-06 — grep-confirmed no such functionality exists in the codebase).
    (b) Add a new closing CTA banner section AFTER the existing Final CTA section (~lines 189-209) and BEFORE the position where the inline footer was (Task 3 deletes it). Structure per UI-SPEC:
        - Outer wrapper: `<section className="max-w-screen-xl mx-auto px-4 sm:px-6 py-24">`
        - Banner treatment: a rounded card/panel (`.card` class or a custom `rounded-3xl` treatment), light background (not the dark treatment of the pre-existing Final CTA — the two sections are distinct visual variants per D-10), centered content
        - Heading: t("Don't see a perfect fit?") — H2, using the pre-existing legacy Heading weight/size treatment (text-3xl font-bold) since it's a section H2, per UI-SPEC pre-existing exception
        - Body: t("Tell us about your sourcing volume and team size — we'll help you pick the right plan, or scope a custom one.")
        - Button: t("Talk to sales") using the `.btn-cta` amber-accent class; `<Link href="mailto:${COMPANY.contactEmail}">` (or a plain `<a>`); this is the 4th and final amber-accent site on the landing page
    (c) Every new string on this page goes through t(...) per the existing useT() pattern (PATTERNS.md i18n rule). Confirm t() is already imported at the top of the file (it is — via useT() from @/components/LanguageProvider).
    NEVER add a second amber-accent CTA to any tile in the feature grid (feature-grid tile CTAs are not amber-accented per UI-SPEC Color — accent is reserved for the 4 declared sites). NEVER add an RFP & Intake Tools tile or an ERP/API Integrations tile (D-06 exclusion). NEVER paraphrase the Supplier Marketplace tile copy (MKT-04 D-06 locked copy).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10 && grep -c 'AI discovers and verifies suppliers live from the web' components/LandingContent.tsx && grep -c "Don't see a perfect fit" components/LandingContent.tsx && grep -c 'Talk to sales' components/LandingContent.tsx && test $(grep -c 'RFP & Intake Tools' components/LandingContent.tsx) -eq 0 && test $(grep -c 'ERP/API Integrations' components/LandingContent.tsx) -eq 0 && test $(grep -cE 'Autonomous Sourcing Engine|Compliance & Audit|Workflow Automation|Collaboration Hub|Budget & Spend Intelligence|Supplier Marketplace' components/LandingContent.tsx) -ge 6</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0
    - The exact string "AI discovers and verifies suppliers live from the web" appears in components/LandingContent.tsx (Supplier Marketplace repositioned copy, MKT-04 D-06 locked)
    - The exact string "Don't see a perfect fit" appears in components/LandingContent.tsx (closing CTA heading, MKT-01)
    - The exact string "Talk to sales" appears in components/LandingContent.tsx (closing CTA button label)
    - The strings "RFP & Intake Tools" and "ERP/API Integrations" do NOT appear anywhere in components/LandingContent.tsx (excluded per D-06)
    - The feature-grid section renders exactly 6 tiles between the "How it works" and "Pricing" sections
    - Every new user-facing string is wrapped in t() via useT()
    - No tile CTA uses the `.btn-cta` class (amber accent restricted to the 4 declared sites)
  </acceptance_criteria>
  <done>The 6-tile feature grid and the closing CTA banner render on the landing page with the correct copy, structure, and typography weights; MKT-01 and MKT-04 acceptance criteria are met from a content-shipped standpoint (visual verification happens in plan 02-04).</done>
</task>

<task type="auto">
  <name>Task 3: Delete the duplicate inline footer block from LandingContent.tsx</name>
  <files>components/LandingContent.tsx</files>
  <read_first>
    - components/LandingContent.tsx (post-Task-2 state — lines 211-228 are the inline <footer> block being deleted, though line numbers may have shifted after Tasks 1-2)
    - components/SiteFooter.tsx (the global footer at app/layout.tsx that will be extended in plan 02-03 — this is the sole remaining footer after this task)
    - app/layout.tsx (lines 60-77 — confirms SiteFooter is rendered globally under the {children} slot; deleting the inline duplicate leaves SiteFooter as the sole footer on every route including /)
    - .planning/phases/02-marketing-pricing-surface/02-RESEARCH.md section "Pattern 3: Footer + closing CTA reconciliation" (fully documents the duplicate-footer bug this task fixes)
  </read_first>
  <behavior>
    - Test 1 (visual/manual, plan 02-04): The rendered / page shows exactly ONE footer (the global SiteFooter.tsx) — the pre-existing stacked-two-footers bug is fixed
    - Test 2: The Sparkles-icon-in-blue-rounded-square logo mark that was inline in the deleted footer is NOT referenced elsewhere in components/LandingContent.tsx after this task (its usage is transferred to SiteFooter.tsx in plan 02-03, not duplicated back into this file)
    - Test 3: `npm run build` exits 0 — no dangling imports or broken JSX after the deletion
  </behavior>
  <action>
    Delete the entire inline `<footer>` block from components/LandingContent.tsx. Currently that block starts around line 212 with `<footer className="border-t border-slate-100">` and ends around line 228 with `</footer>` (line numbers may have shifted after Tasks 1-2 — locate by the opening `<footer>` tag near the end of the JSX return, not by absolute line number).
    Also remove any imports that were exclusively used inside the deleted footer if they are unused elsewhere in the file after deletion — but ONLY if they are unused; do not remove `Sparkles` (from lucide-react) if it is still used elsewhere in the file (it may still be referenced in the Hero or feature-grid tiles from Tasks 1-2). Run the build after the deletion to catch any unused-import lint warning.
    Do NOT delete anything outside the `<footer>...</footer>` block — the closing CTA banner from Task 2 is a separate `<section>` and must remain.
    NEVER add footer content back into components/LandingContent.tsx after deleting it — plan 02-03 extends SiteFooter.tsx (the correct architectural home per PATTERNS.md and RESEARCH.md). NEVER leave a hidden/commented-out remnant of the deleted footer; delete the block entirely.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10 && test $(grep -cE '^\s*<footer\b' components/LandingContent.tsx) -eq 0 && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0
    - npm run lint exits 0 (no unused-import warnings introduced by the deletion)
    - Zero `<footer>` opening tags in components/LandingContent.tsx (the inline footer is fully deleted)
    - The Copyright, "Privacy Policy" link, and "Terms of Service" link that used to appear in the inline footer are no longer duplicated in LandingContent (they still appear once, in SiteFooter.tsx, which plan 02-03 extends further)
  </acceptance_criteria>
  <done>The duplicate inline footer is deleted from LandingContent.tsx; the global SiteFooter (plan 02-03's extension target) is now the sole footer on every route. Build and lint remain clean.</done>
</task>

</tasks>

<verification>
Per-task automated verify above. Cross-task:
- `npm run build` passes (the primary compile-time catch for Pitfall 3 lucide-react brand-icon imports, missing static assets, and TypeScript field-rename regressions)
- `npm run typecheck` passes
Full phase verification (typecheck + lint + test + build + human visual checks) runs in plan 02-04.
</verification>

<success_criteria>
- Landing page renders 4 tier cards driven live by lib/plans.ts (Basic/Growth/Premium/Enterprise, no `free`)
- Basic card is the only "Start free trial" entry; Enterprise card is Contact-sales mailto
- New 6-tile feature grid section renders with the specified copy
- New "Don't see a perfect fit?" closing CTA banner renders after Final CTA and before global footer
- Hero has a real <img> element referencing the newly-committed public/hero-placeholder.svg
- Inline duplicate footer is deleted; global SiteFooter is the sole footer
</success_criteria>

<output>
Create `.planning/phases/02-marketing-pricing-surface/02-02-SUMMARY.md` when done, documenting: per-tile copy shipped (all 6 tiles), grid column layout chosen (3-wide desktop / 2-wide tablet / stacked mobile), choice of lucide-react icons per tile, whether any hardcoded fake-data content in the Hero was preserved as visual fallback or removed entirely.
</output>
