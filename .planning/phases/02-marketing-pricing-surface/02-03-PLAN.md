---
phase: 02-marketing-pricing-surface
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/SiteFooter.tsx
  - components/icons/SocialIcons.tsx
  - app/legal/ccpa/page.tsx
autonomous: true
requirements: [MKT-02, MKT-03]

estimate:
  tokens: 45000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "As a buyer visiting any page on SourceGPT, I want to see a footer with logo, mission tagline, contact email, social icons (Facebook, Instagram, LinkedIn), Privacy/Terms/CCPA legal links, and a copyright line, so that I can find company info and legal pages from every route (MKT-02)"
    - "As a California resident, I want to visit /legal/ccpa and read a real CCPA disclosure describing categories of data collected, my rights (know/delete/opt-out/non-discrimination), and how to exercise them, so that I understand my rights before or after using the product (MKT-03, D-07)"
    - "components/SiteFooter.tsx renders logo (Sparkles-icon-in-blue-rounded-square + 'SourceGPT'), tagline, mailto contact link sourced from COMPANY.contactEmail (lib/legal.ts), 3 social icons (Facebook, Instagram, LinkedIn) each rendered as inline SVG via the new components/icons/SocialIcons.tsx module, 3 legal links (Privacy, Terms, CCPA), and copyright — all in one <footer> element (D-09)"
    - "The 3 social icons are inline SVG components authored in components/icons/SocialIcons.tsx — NOT imported from lucide-react (which does not export Facebook/Instagram/Linkedin in v1.x — Pitfall 3), NOT imported from any newly-added npm package (@icons-pack/react-simple-icons returned a SUS legitimacy verdict, per RESEARCH Package Legitimacy Audit)"
    - "Each social-icon anchor carries an aria-label naming the destination platform ('Follow SourceGPT on Facebook' / '...on Instagram' / '...on LinkedIn'); the inner <svg> is marked aria-hidden=\"true\" (UI-SPEC Registry Safety — Accessibility fallback)"
    - "app/legal/ccpa/page.tsx exists as a Next.js server component (no 'use client' directive), exports a Metadata object with title 'CCPA Policy — SourceGPT' and a description, and renders the CCPA content inside a <LegalLayout> wrapper identical to app/legal/privacy/page.tsx's structure (D-07, MKT-03)"
    - "The CCPA page content includes: (a) categories of personal information collected (may reuse Privacy §2's list); (b) CCPA rights section listing right to know, right to delete, right to opt-out of sale/sharing (explicitly note SourceGPT does not sell personal data), and right to non-discrimination; (c) how-to-exercise-rights section with the contact mailto (COMPANY.contactEmail) — real content, not placeholder, per D-07"
    - "components/SiteFooter.tsx's link row uses `flex-wrap` (with `gap-4` or `gap-3`) so on narrow viewports the extended legal-links + social-icons row wraps to a second line rather than clipping or horizontal-scrolling (UI-SPEC UI Considerations overflow row)"
    - { statement: "The SiteFooter renders identically on every route (/, /dashboard, /billing, /legal/*, /events/*) because it's mounted globally in app/layout.tsx — no route-specific footer variation exists after this plan lands (adjacency edge lifted per MKT-02: same footer everywhere)", verification: backstop }
    - { statement: "The CCPA page mailto contact link uses the exact same COMPANY.contactEmail value that Privacy/Terms use — a single source of truth from lib/legal.ts, no local hardcoded email (transparency: contact email must not diverge between legal pages)", verification: backstop }
  prohibitions:
    - "MUST NOT claim on the CCPA page that the document has been reviewed by legal counsel or that it is a certified legal statement — per D-07 'No claim of formal legal review'. The page is a real, genuine disclosure adapted to SourceGPT's actual data practices, not a lawyer-attested filing; overstating its authority would be a transparency violation (misleading users about the document's provenance)."
    - "MUST NOT freehand-trace the Facebook / Instagram / LinkedIn brand icon SVG path data — source path data from Simple Icons (simple-icons.org, canonical monochrome paths) or each brand's official press-kit SVG at implementation time (transparency: freehand-traced brand marks risk misrepresenting the brand and being visually wrong / off-brand — RESEARCH Don't Hand-Roll)."
    - "MUST NOT import Facebook, Instagram, or Linkedin (or any social brand icon) from lucide-react — Lucide v1.x removed brand icons for trademark/bundle-size reasons; the import fails at TypeScript compile time (Pitfall 3). Use the inline SocialIcons.tsx components created in this plan."
  artifacts:
    - "components/SiteFooter.tsx (modified) — extended in place from 19 lines to a full footer with logo, tagline, contact mailto, 3 social icons, 3 legal links (Privacy/Terms/CCPA), copyright, flex-wrap handling"
    - "components/icons/SocialIcons.tsx (new) — 3 named exports (FacebookIcon, InstagramIcon, LinkedinIcon) as inline SVG React components, monochrome (fill=currentColor), 24×24 viewBox, aria-hidden by default, spreadable SVGProps"
    - "app/legal/ccpa/page.tsx (new) — Next.js server component mirroring app/legal/privacy/page.tsx structure, using LegalLayout/LegalSection/LegalList components, real CCPA disclosure content"
  key_links:
    - "SiteFooter.tsx is rendered ONCE globally by app/layout.tsx (lines 60-77) — extending it here means every route (/, /dashboard, /billing, /legal/*, /events/*) gets the extended footer at once, no per-route routing"
    - "Deleting the inline <footer> from LandingContent.tsx happens in plan 02-02 Task 3 (parallel plan, same wave) — these two plans MUST both land for the duplicate-footer bug to be fixed; landing one without the other leaves either (a) a broken/empty landing page footer (02-03 without 02-02) or (b) an under-featured landing page footer (02-02 without 02-03). They are independent files so they can run in parallel, but they are semantically coupled and must be reconciled at wave-merge time"
    - "The CCPA legal link added to SiteFooter here (href=/legal/ccpa) MUST point to the new page created in this same plan's Task 3 — verify the URL and route match; a mismatch produces a footer link to a 404"
    - "components/icons/SocialIcons.tsx is a new pattern in the repo (no existing icon-wrapper analog per PATTERNS.md 'No Analog Found'); its shape follows components/legal/LegalLayout.tsx's small-typed-component convention (plain-props-in/JSX-out, named exports for sub-parts)"

artifacts_produced:
  - "FacebookIcon component (named export from components/icons/SocialIcons.tsx)"
  - "InstagramIcon component (named export from components/icons/SocialIcons.tsx)"
  - "LinkedinIcon component (named export from components/icons/SocialIcons.tsx)"
  - "SiteFooter extended sections: logo mark, tagline, contact mailto, social icon row, CCPA link"
  - "/legal/ccpa route (new Next.js server component at app/legal/ccpa/page.tsx)"
  - "New directory: components/icons/"
---

<objective>
Extend the global SiteFooter with full MKT-02 content, author the new inline social-icon
components (Pitfall 3 workaround), and create the new /legal/ccpa page mirroring
Privacy/Terms structure. Runs in parallel with plan 02-02 (LandingContent overhaul) —
different files entirely, both depend on 02-01's tier catalog only for CTA consistency.

Purpose: MKT-02 (footer content) and MKT-03 (CCPA page) land here as new site-wide
infrastructure. The extended SiteFooter is the correct architectural home for footer
content per RESEARCH Pattern 3, replacing the duplicate inline footer that plan 02-02
Task 3 deletes.

Output: components/SiteFooter.tsx heavily extended; new components/icons/SocialIcons.tsx
with 3 brand icons; new app/legal/ccpa/page.tsx server component. Every route in the
app now has the extended footer; /legal/ccpa returns a rendered CCPA policy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-marketing-pricing-surface/02-CONTEXT.md
@.planning/phases/02-marketing-pricing-surface/02-RESEARCH.md
@.planning/phases/02-marketing-pricing-surface/02-PATTERNS.md
@.planning/phases/02-marketing-pricing-surface/02-UI-SPEC.md
@components/SiteFooter.tsx
@components/LanguageProvider.tsx
@components/legal/LegalLayout.tsx
@app/legal/privacy/page.tsx
@app/legal/terms/page.tsx
@app/layout.tsx
@lib/legal.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CCPA page content -> user | Server-rendered static content; no user input reaches it; the mailto link is a client-side protocol handoff, no cross-site risk |
| Social icon SVG paths | Rendered inline (dangerouslySetInnerHTML NOT used — JSX SVG only); no dynamic path data, no XSS surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Repudiation | CCPA policy content claiming legal review that has not occurred | medium | mitigate | Prohibition item (this plan): the page must not claim legal-counsel review; content is a genuine adaptation of SourceGPT's actual data practices per D-07. Reviewed at plan 02-04 human checkpoint. |
| T-02-06 | Tampering | Freehand-traced or off-brand social-icon SVG path data misrepresenting a third-party brand | low | mitigate | Prohibition item (this plan): path data must be sourced from Simple Icons or the brand's own press kit at implementation time, not freehand-traced. Reviewed at plan 02-04 human checkpoint. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create components/icons/SocialIcons.tsx with 3 inline-SVG brand icon components</name>
  <files>components/icons/SocialIcons.tsx</files>
  <read_first>
    - components/legal/LegalLayout.tsx (full 58-line file — the structural template for a small typed presentational component with named exports)
    - .planning/phases/02-marketing-pricing-surface/02-RESEARCH.md Pitfall 3 (lucide-react brand-icon removal), Package Legitimacy Audit (@icons-pack/react-simple-icons SUS verdict — do not add), Don't Hand-Roll table (canonical monochrome paths sourced from Simple Icons or brand press kits, not freehand)
    - .planning/phases/02-marketing-pricing-surface/02-UI-SPEC.md Registry Safety section (aria-label on anchor, aria-hidden on SVG — accessibility fallback)
    - .planning/phases/02-marketing-pricing-surface/02-PATTERNS.md section for components/icons/SocialIcons.tsx (No Analog Found — new pattern; small-typed-component shape from LegalLayout.tsx)
    - https://simple-icons.org (external — source Facebook/Instagram/LinkedIn canonical monochrome SVG path data at implementation time; NEVER freehand-trace)
  </read_first>
  <behavior>
    - Test 1: `import { FacebookIcon, InstagramIcon, LinkedinIcon } from "@/components/icons/SocialIcons"` succeeds and TypeScript recognizes each as `(props: React.SVGProps<SVGSVGElement>) => JSX.Element`
    - Test 2: Each component renders an <svg> element with viewBox="0 0 24 24", `fill="currentColor"`, `aria-hidden="true"`, and spreads incoming props (so className/width/height passed by SiteFooter propagates)
    - Test 3: Each <svg> contains at least one <path> element with a real `d=` attribute — path data sourced from Simple Icons or brand press kits (not empty, not a placeholder square), so the icon renders as the correct brand mark (transparency: NOT a freehand trace)
    - Test 4: `npm run build` and `npm run typecheck` exit 0
  </behavior>
  <action>
    Create a new file components/icons/SocialIcons.tsx (also create the components/icons/ directory — it does not exist yet).
    (a) The file has NO "use client" directive — these are pure presentational components that work in both server and client components (SiteFooter is a client component per its existing "use client", but the icons themselves don't need it).
    (b) Do NOT import from lucide-react and do NOT add any new npm package (Pitfall 3; Package Legitimacy — @icons-pack/react-simple-icons is SUS).
    (c) Export three named components: FacebookIcon, InstagramIcon, LinkedinIcon. Each has signature `(props: React.SVGProps<SVGSVGElement>) => JSX.Element` and follows this shape:
        - <svg> wrapper: viewBox="0 0 24 24", fill="currentColor", aria-hidden="true", spread {...props} so callers pass className / width / height / etc.
        - One or more <path> elements with real `d=` attribute path data — SOURCE the path data from Simple Icons (simple-icons.org — canonical monochrome brand SVGs, licensed CC0) at implementation time. Copy the exact `<path d="...">` string into each icon. DO NOT freehand-trace. DO NOT approximate — if the Simple Icons SVG uses multiple paths, include all of them.
    (d) File header comment: a short block explaining why lucide-react is not used (Pitfall 3 v1.x brand-icon removal) and where the path data was sourced from (Simple Icons + version/date if useful). This preserves institutional memory when someone later wonders "why don't we just use lucide?"
    (e) The components must accept and spread SVGProps so SiteFooter can pass `className="w-4 h-4"` and control size at the call site.
    NEVER import Facebook/Instagram/Linkedin from lucide-react. NEVER add a new npm dependency. NEVER commit the file with empty <svg /> placeholder path data (the icons would render blank and MKT-02 would silently miss the "social icons" requirement).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -cE 'SocialIcons\.tsx' | grep -q '^0$' && test -f components/icons/SocialIcons.tsx && grep -cE 'export function (Facebook|Instagram|Linkedin)Icon' components/icons/SocialIcons.tsx && grep -cE 'viewBox="0 0 24 24"' components/icons/SocialIcons.tsx && grep -cE 'aria-hidden="true"' components/icons/SocialIcons.tsx && test $(grep -cE 'from "lucide-react".*Facebook|from "lucide-react".*Instagram|from "lucide-react".*Linkedin' components/icons/SocialIcons.tsx) -eq 0 && grep -cE '<path d="' components/icons/SocialIcons.tsx</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0 with no errors targeting components/icons/SocialIcons.tsx
    - components/icons/SocialIcons.tsx exists as a file
    - The three exports (FacebookIcon, InstagramIcon, LinkedinIcon) are present as named function exports
    - Each icon's <svg> has viewBox="0 0 24 24" and aria-hidden="true"
    - Each icon has at least one <path d="..."> element with non-empty path data (real brand-mark geometry sourced from Simple Icons, not an empty/placeholder shape)
    - No lucide-react import of Facebook/Instagram/Linkedin in this file
    - No new npm dependency added to package.json
  </acceptance_criteria>
  <done>components/icons/SocialIcons.tsx exports 3 real inline-SVG brand icons ready to be consumed by the extended SiteFooter in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend components/SiteFooter.tsx with logo, tagline, contact, social icons, CCPA link, and flex-wrap handling</name>
  <files>components/SiteFooter.tsx</files>
  <read_first>
    - components/SiteFooter.tsx (current 19-line file — extending in place, not rebuilding)
    - components/icons/SocialIcons.tsx (post-Task-1 state — importing the 3 icons)
    - lib/legal.ts (source of COMPANY, COMPANY.contactEmail, COMPANY.legalName — used for the tagline/contact and any legal-entity references)
    - components/LanguageProvider.tsx (useT() pattern for i18n-wrapping all new user-facing strings)
    - components/LandingContent.tsx (lines 214-218 specifically — the Sparkles-icon-in-blue-rounded-square logo mark being borrowed; the block will be deleted from LandingContent in plan 02-02 Task 3 but its structure is the visual template)
    - .planning/phases/02-marketing-pricing-surface/02-UI-SPEC.md sections "Registry Safety" (aria-label pattern per icon anchor), "UI Considerations" (overflow: footer link row uses flex-wrap), "Color" (secondary-30% palette for footer border/background — no amber in footer)
    - .planning/phases/02-marketing-pricing-surface/02-PATTERNS.md section for components/SiteFooter.tsx (logo mark to borrow, legal-links pattern, contact-email pattern, overflow handling)
    - app/legal/privacy/page.tsx (line 11 — the `mailto` helper pattern already used in privacy, mirror the same approach for the footer contact link)
  </read_first>
  <behavior>
    - Test 1 (visual/manual, plan 02-04): The footer on every page (/, /dashboard, /billing, /legal/*) now shows: (1) Sparkles logo in blue rounded square + "SourceGPT" wordmark; (2) mission tagline (one-line, e.g. "AI-powered supplier intelligence."); (3) contact email as a mailto link; (4) 3 social icons (Facebook/Instagram/LinkedIn) as icon-only links with aria-label on each anchor; (5) 3 legal links: Privacy Policy, Terms of Service, CCPA Policy; (6) copyright line
    - Test 2 (visual): On narrow viewports (mobile), the extended row wraps with flex-wrap rather than clipping or horizontal-scrolling — the footer remains usable at 375px width (UI-SPEC overflow row)
    - Test 3 (visual): Each social-icon anchor's aria-label reads "Follow SourceGPT on Facebook" / "...on Instagram" / "...on LinkedIn"; the <svg> inside is aria-hidden — a screen reader announces only the anchor's aria-label, not a duplicate SVG title
    - Test 4: `npm run build` and `npm run typecheck` exit 0
    - Test 5: The href on the CCPA legal link is exactly "/legal/ccpa" (the route created in Task 3)
  </behavior>
  <action>
    Extend components/SiteFooter.tsx in place (do NOT rebuild — start from the current 19-line file and add):
    (a) Preserve the top-level "use client" directive, the useT() import from @/components/LanguageProvider, and the Link import from next/link. Add these imports: `Sparkles` from lucide-react (safe — non-brand icon, present in v1.x); `FacebookIcon`, `InstagramIcon`, `LinkedinIcon` from `@/components/icons/SocialIcons`; `COMPANY` from `@/lib/legal`.
    (b) Restructure the <footer> children to include (roughly top-to-bottom or left-to-right depending on responsive layout — Claude's discretion within UI-SPEC spacing):
        - Logo mark: `<div className="flex items-center gap-2 text-sm font-bold text-slate-900"><div className="w-6 h-6 rounded-lg bg-blue-600 flex items-center justify-center"><Sparkles className="w-3.5 h-3.5 text-white" /></div>SourceGPT</div>` (borrowed from the deleted inline footer)
        - Mission tagline: `<p className="text-xs text-slate-500">{t("AI-powered supplier intelligence.")}</p>` (the existing copyright already contains this string — extract it into a separate tagline line above the copyright, so the tagline is a first-class visible element)
        - Contact mailto: `<a href={`mailto:${COMPANY.contactEmail}`} className="text-xs text-slate-500 hover:text-slate-900 transition-colors">{COMPANY.contactEmail}</a>` (using the existing mailto helper pattern from app/legal/privacy/page.tsx:11)
        - Social icons row: 3 icon-only anchors, each `<a href="{platform-url-placeholder}" aria-label="Follow SourceGPT on {Platform}" className="text-slate-400 hover:text-slate-700 transition-colors">{Icon className="w-4 h-4"}</a>` — the href can be a placeholder URL like "https://facebook.com/sourcegpt" (or "#" if no live social account exists yet — Claude's discretion; the requirement is that the icons render and are labeled, not that the destinations exist in the wild)
        - Legal links row: `<Link href="/legal/privacy" ...>{t("Privacy Policy")}</Link>`, `<Link href="/legal/terms" ...>{t("Terms of Service")}</Link>`, `<Link href="/legal/ccpa" ...>{t("CCPA Policy")}</Link>` (new link) — same className/hover treatment as the existing 2 links
        - Copyright: `<p className="text-xs text-slate-500">© {new Date().getFullYear()} SourceGPT.</p>` (extracted from the current combined-with-tagline line above)
    (c) Container: wrap all rows in the existing `<div className="max-w-screen-2xl mx-auto px-4 sm:px-6 py-5 ...">` outer wrapper (existing structure preserved). The inner rows use `flex flex-wrap items-center justify-between gap-4` (or `gap-3`) on the outer flex container so children wrap to a second line on narrow viewports — this is the UI-SPEC overflow requirement.
    (d) Every user-facing string wrapped in t(...) via useT() (i18n rule per PATTERNS.md). Social-icon aria-labels can remain plain English (accessibility metadata is not part of the visible-copy i18n contract per PATTERNS.md's SocialIcons section).
    NEVER import Facebook/Instagram/Linkedin from lucide-react (Pitfall 3). NEVER add amber-accent styling to any footer element — accent is reserved for the 4 landing-page CTAs per UI-SPEC. NEVER hardcode the contact email — read from COMPANY.contactEmail so a future rename of the address flows through automatically.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10 && grep -c 'FacebookIcon' components/SiteFooter.tsx && grep -c 'InstagramIcon' components/SiteFooter.tsx && grep -c 'LinkedinIcon' components/SiteFooter.tsx && grep -c '"/legal/ccpa"' components/SiteFooter.tsx && grep -c 'COMPANY.contactEmail' components/SiteFooter.tsx && grep -cE 'aria-label="Follow SourceGPT' components/SiteFooter.tsx && grep -cE 'flex-wrap' components/SiteFooter.tsx</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0
    - Non-comment occurrences of FacebookIcon, InstagramIcon, LinkedinIcon in components/SiteFooter.tsx each >= 1
    - "/legal/ccpa" appears as a link href in components/SiteFooter.tsx exactly 1 time
    - COMPANY.contactEmail is used (not a hardcoded email address) at least once for the contact mailto
    - At least 3 aria-label attributes starting with "Follow SourceGPT" (one per social-icon anchor)
    - The outer footer flex container includes `flex-wrap` for the UI-SPEC overflow requirement
    - The mission tagline "AI-powered supplier intelligence." is a separate visible element from the copyright line
  </acceptance_criteria>
  <done>components/SiteFooter.tsx renders logo + tagline + contact + 3 social icons + 3 legal links + copyright on every route, with flex-wrap on narrow viewports; MKT-02 is fully addressed from a component-shipped standpoint (visual verification in plan 02-04).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create app/legal/ccpa/page.tsx mirroring the Privacy/Terms pattern with real CCPA content</name>
  <files>app/legal/ccpa/page.tsx</files>
  <read_first>
    - app/legal/privacy/page.tsx (full file, 160 lines — the exact structural template to mirror: imports, Metadata export, LegalLayout wrapper, LegalSection/LegalList nesting, mailto helper)
    - app/legal/terms/page.tsx (structurally identical — spot-read to confirm the pattern)
    - components/legal/LegalLayout.tsx (58 lines — consumed as-is; no changes needed here)
    - lib/legal.ts (source of COMPANY, COMPANY.legalName, COMPANY.contactEmail, LEGAL_LAST_UPDATED)
    - .planning/phases/02-marketing-pricing-surface/02-CONTEXT.md D-07 (CCPA content requirements: standard disclosure, right to know/delete/opt-out, non-discrimination, adapted to SourceGPT's actual data practices, no claim of formal legal review)
    - .planning/phases/02-marketing-pricing-surface/02-RESEARCH.md section "Pattern 4: New legal page (CCPA)" including the starter skeleton
    - .planning/phases/02-marketing-pricing-surface/02-PATTERNS.md section for app/legal/ccpa/page.tsx (mirror privacy imports/metadata/component/mailto/section/list patterns)
    - app/layout.tsx (title template — "%s — SourceGPT" — the CCPA page's metadata.title must match this template)
  </read_first>
  <behavior>
    - Test 1: Visiting /legal/ccpa in a running dev server renders a page with:
        - Page title "CCPA Policy — SourceGPT" (Next.js Metadata)
        - Heading "CCPA Policy" (via LegalLayout)
        - Intro paragraph naming COMPANY.legalName and the product
        - At least 3 <LegalSection> blocks: categories of data collected, CCPA rights (know/delete/opt-out/non-discrimination), how to exercise (mailto contact)
        - Back link to /dashboard (inherited from LegalLayout — automatic)
    - Test 2: The page is a server component (no "use client" directive at the top of the file)
    - Test 3: The page imports LegalLayout, LegalSection, LegalList from "@/components/legal/LegalLayout" (same as privacy)
    - Test 4: The page imports COMPANY from "@/lib/legal" and uses COMPANY.contactEmail in the how-to-exercise section (no hardcoded email address)
    - Test 5: `npm run build` exits 0 (Next.js detects the new route and successfully builds it)
    - Test 6: `curl -sI http://localhost:3000/legal/ccpa` (in a running dev server, verified at plan 02-04's human checkpoint) returns 200 OK, not 404
  </behavior>
  <action>
    Create app/legal/ccpa/page.tsx. Mirror app/legal/privacy/page.tsx's structure exactly (that's the exact template per PATTERNS.md Pattern 4).
    (a) File header: no "use client" directive (server component, matches privacy/terms).
    (b) Imports:
        import type { Metadata } from "next";
        import LegalLayout, { LegalSection, LegalList } from "@/components/legal/LegalLayout";
        import { COMPANY } from "@/lib/legal";
    (c) Metadata export:
        export const metadata: Metadata = {
          title: "CCPA Policy — SourceGPT",
          description: "Your California privacy rights under the CCPA/CPRA and how SourceGPT honors them.",
        };
        (matches app/layout.tsx's "%s — SourceGPT" template convention)
    (d) Default-export component:
        export default function CcpaPolicy() {
          const mailto = (addr: string) => <a href={`mailto:${addr}`}>{addr}</a>;
          return (
            <LegalLayout
              title="CCPA Policy"
              intro={`This notice supplements ${COMPANY.legalName}'s Privacy Policy for California residents...`}
            >
              {/* sections below */}
            </LegalLayout>
          );
        }
    (e) Author 3-5 real <LegalSection> blocks with real CCPA content adapted to SourceGPT's data practices per D-07. Suggested outline:
        Section 1: "Categories of personal information we collect" — LegalList of the same categories as Privacy Policy §2 (account data, billing data, sourcing data, communications data, usage data). May reuse or reference privacy §2's list; do not fabricate categories the app does not actually collect.
        Section 2: "Your CCPA rights" — LegalList of the four core CCPA rights:
            - "Right to know what personal information is collected, used, and disclosed."
            - "Right to delete personal information, subject to certain exceptions."
            - "Right to opt out of the sale or sharing of personal information — SourceGPT does not sell personal data."
            - "Right to non-discrimination for exercising your CCPA rights."
        Section 3: "How to exercise your rights" — a paragraph explaining that requests can be sent to the contact email, via {mailto(COMPANY.contactEmail)}. State the verification process at a high level (SourceGPT will verify the request is from the identified individual before honoring it).
        Optional Section 4: "Notice at collection" — one paragraph stating what categories are collected at signup and their business purpose.
        Optional Section 5: "Financial incentives" — a one-line statement that SourceGPT does not offer financial incentives in exchange for personal information (if true per D-07's "adapted to actual data practices").
    (f) All content is real, adapted to SourceGPT — NOT placeholder/lorem-ipsum. But do NOT claim that the document has been reviewed by counsel or is a certified legal statement (D-07, T-02-05 mitigation).
    NEVER copy content that describes practices SourceGPT does not actually engage in (do not claim we honor a right the product cannot honor). NEVER hardcode COMPANY.legalName or COMPANY.contactEmail — read from lib/legal.ts so a future rename flows through. NEVER add a "use client" directive.
  </action>
  <verify>
    <automated>test -f app/legal/ccpa/page.tsx && grep -c 'from "@/components/legal/LegalLayout"' app/legal/ccpa/page.tsx && grep -c 'from "@/lib/legal"' app/legal/ccpa/page.tsx && grep -c 'CCPA Policy' app/legal/ccpa/page.tsx && test $(grep -c '^"use client"' app/legal/ccpa/page.tsx) -eq 0 && grep -c 'Right to know' app/legal/ccpa/page.tsx && grep -c 'Right to delete' app/legal/ccpa/page.tsx && grep -c 'Right to non-discrimination' app/legal/ccpa/page.tsx && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - app/legal/ccpa/page.tsx exists as a file
    - Imports LegalLayout, LegalSection, LegalList from "@/components/legal/LegalLayout"
    - Imports COMPANY from "@/lib/legal"
    - Metadata.title === "CCPA Policy — SourceGPT"
    - File does NOT start with "use client" (server component)
    - Content includes at least the strings "Right to know", "Right to delete", and "Right to non-discrimination" (the three canonical CCPA rights)
    - No hardcoded email address; contact is via mailto(COMPANY.contactEmail) or equivalent
    - npm run build exits 0 (Next.js discovers and builds the new route)
    - No claim of legal-counsel review or certified legal status in the page content (prohibition compliance)
  </acceptance_criteria>
  <done>/legal/ccpa route renders a real CCPA disclosure using the same LegalLayout as Privacy/Terms; the footer's CCPA link (from Task 2) resolves to a live page, not a 404.</done>
</task>

</tasks>

<verification>
Per-task automated verify above. Cross-task:
- `npm run build` passes (Next.js builds the new route; TypeScript catches any SocialIcons.tsx or SiteFooter.tsx import error)
- Full phase verification runs in plan 02-04.
</verification>

<success_criteria>
- Every page in the app now renders the extended footer (logo + tagline + contact + social icons + 3 legal links + copyright)
- /legal/ccpa returns a real CCPA page in the same LegalLayout as Privacy/Terms
- No lucide-react brand-icon import exists anywhere in the codebase (grep-verified)
- No new npm dependency added
</success_criteria>

<output>
Create `.planning/phases/02-marketing-pricing-surface/02-03-SUMMARY.md` when done, documenting: which social-media URLs (real or placeholder) were used for the 3 anchor hrefs, the exact CCPA sections drafted (count and titles), and where Simple Icons path data was sourced (URL + date, for future maintenance).
</output>
