---
phase: 02-marketing-pricing-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/plans.ts
  - app/api/stripe/checkout/route.ts
  - app/billing/page.tsx
  - tests/plans.test.ts
autonomous: true
requirements: [PRICE-01, PRICE-02, PRICE-03, PRICE-04, PRICE-05]

estimate:
  tokens: 55000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "As a buyer, I want to see 3 paid USD tiers plus a Contact-us Enterprise tier so that I can pick the right plan for my volume (per D-02, D-03, D-04)"
    - "getTier('free'), getTier('basic'), getTier('growth'), getTier('premium'), and getTier('pro') all resolve to a valid Tier object with .key === the passed key (PRICE-01, PRICE-05, Pitfall 1 — the 'free' key MUST be preserved as internal fallback)"
    - "TIERS array contains exactly one tier with contactSales === true, whose key is 'pro' and whose name is 'Enterprise' (D-02: 'pro' tier retired/folded into 'Contact us' Enterprise, key unchanged)"
    - "Tier.monthlyUsd for Basic falls within [1400, 1500] inclusive; Growth/Basic ratio is within [1.5, 2.0]; Premium/Growth ratio is within [1.5, 2.0] (PRICE-02)"
    - "displayPrice(basic, 'monthly') returns basic.monthlyUsd; displayPrice(basic, 'yearly') returns Math.round(basic.monthlyUsd * 12 * (1 - YEARLY_DISCOUNT)); displayPrice(basic, 'weekly') returns Math.round((basic.monthlyUsd / 4.33) * (1 + WEEKLY_PREMIUM))"
    - "displayPrice returns 0 for the 'free' tier at every cadence, and returns 0 for any tier where contactSales === true (Pitfall 2: contactSales tiers must not render a numeric price)"
    - "POST /api/stripe/checkout with { tier: 'pro', cadence: 'monthly' } returns HTTP 400 with error 'Invalid plan selected' (Enterprise/contactSales tier cannot self-serve-checkout — Security Domain threat T-02-01)"
    - "POST /api/stripe/checkout with { tier: 'free', cadence: 'monthly' } still returns HTTP 400 with error 'Invalid plan selected' (pre-existing behavior preserved)"
    - "priceEnvVar('basic', 'weekly') returns 'STRIPE_PRICE_BASIC_WEEKLY'; priceEnvVar('growth', 'monthly') returns 'STRIPE_PRICE_GROWTH_MONTHLY'; priceEnvVar('premium', 'yearly') returns 'STRIPE_PRICE_PREMIUM_YEARLY' (PRICE-04 env-var matrix contract)"
    - { statement: "The tier ordering in TIERS (as an ordered array) places tiers in strictly ascending monthlyUsd for the three self-serve paid tiers: basic monthlyUsd less than growth monthlyUsd less than premium monthlyUsd (ordering-precision edge lifted per PRICE-04 boundary+precision probe)", verification: backstop }
    - { statement: "For a Stripe Checkout Session created when tierKey === 'basic', the createSession call includes subscription_data.trial_period_days === 14; for tierKey in { 'growth', 'premium' } the call does NOT include trial_period_days (PRICE-03: free trial ONLY on Basic — the discriminating condition is tierKey === 'basic', not any other flag)", verification: backstop }
    - "effectiveTier(org) returns a valid Tier and does not throw for org.plan values 'free', 'basic', 'growth', 'premium', 'pro', 'trial', and an arbitrary unknown string 'legacy-plan-key' (PRICE-05: no billing-gate outage — regression guard on lib/usage.ts:144-152's getTier('free')! fallback; D-01: no real paying customers yet, so this is a lightweight defensive check against orphaned plan values, not a migration project)"
  prohibitions:
    - "MUST NOT delete the 'free' key from TierKey union or from the TIERS array — lib/usage.ts:144-152's effectiveTier() does a non-null-asserted getTier('free')! that will throw at runtime on any canceled subscriber if the key is removed; and app/api/stripe/webhook/route.ts:33 explicitly writes plan='free' on cancellation, so 'free' is an actively-written resting state (Pitfall 1, transparency: silent runtime crash for existing dev/test orgs would violate PRICE-05's 'no billing-gate outage' intent)"
    - "MUST NOT set the Enterprise/'pro' tier's monthlyUsd to 0 as a shorthand for 'no listed price' — the billing page and displayPrice already special-case price zero to mean 'Free' with a 'No card required' message; reusing that branch would render Enterprise as 'Free' in production UI (Pitfall 2, transparency: users would see a wrong price, an accidental misrepresentation of paid enterprise pricing). Use the contactSales discriminator field instead."
  artifacts:
    - "lib/plans.ts (modified) — Tier.monthlyEur renamed to monthlyUsd; contactSales?: boolean added; TIERS entries repriced/relimited per D-02/D-03; 'free' key preserved"
    - "app/api/stripe/checkout/route.ts (modified) — validation rejects contactSales tiers; createSession sets trial_period_days: 14 only for tierKey === 'basic'"
    - "app/billing/page.tsx (modified) — currency symbol EUR to USD; contactSales branch renders 'Contact sales' CTA before Free branch; TIERS.filter(t => t.key !== 'free') applied to comparison grid"
    - "tests/plans.test.ts (modified) — assertions updated for monthlyUsd field name, new tier limit numbers, contactSales discrimination; new defensive test asserts effectiveTier() never throws"
  key_links:
    - "lib/plans.ts is the single source of truth for tier data; LandingContent.tsx (plan 02-02), app/billing/page.tsx (this plan), lib/billing.ts, and lib/usage.ts all read it — a rename or shape change here must land in the same commit as its consumers, not staged separately"
    - "The 'free' tier key is the load-bearing internal fallback for lib/usage.ts:144-152 (getTier('free')! non-null assertion) and app/api/stripe/webhook/route.ts:33 (plan='free' on cancellation). Removing it silently crashes canceled subscribers — verified by RESEARCH.md Pitfall 1."
    - "The trial-branch condition in createSession must be tierKey === 'basic', NOT tier.contactSales === false or !tier.featured — those would grant trials to Growth/Premium too. This is the exact 'PRICE-03: free trial only on base paid tier' discriminator (D-05)."

artifacts_produced:
  - "Tier.monthlyUsd field (renamed from monthlyEur)"
  - "Tier.contactSales optional boolean discriminator field"
  - "Enterprise display name on the 'pro' TierKey entry (key unchanged)"
  - "displayPrice() leading branch on contactSales returning 0"
  - "STRIPE_PRICE_BASIC_WEEKLY / STRIPE_PRICE_BASIC_MONTHLY / STRIPE_PRICE_BASIC_YEARLY / STRIPE_PRICE_GROWTH_* / STRIPE_PRICE_PREMIUM_* env var names (9 total, resolved via existing priceEnvVar helper — the names themselves are the produced surface even though env values are set by human checkpoint in plan 02-04)"
  - "effectiveTier() defensive regression test (new it block in tests/plans.test.ts OR new file tests/billing-defensive.test.ts)"
---

<objective>
End-to-end tracer slice: rebuild the pricing catalog data layer (lib/plans.ts) to
Basic/Growth/Premium/Enterprise with USD pricing and contactSales discrimination, and
wire it through both consumer surfaces this phase's data touches (Stripe Checkout route
and in-app billing UI) plus the pricing test suite. This proves the catalog shape works
end-to-end before the marketing UI (plan 02-02) reads new fields off it.

**User story (per MVP mode):** **As a** buyer evaluating SourceGPT plans, **I want to**
see three USD-priced self-serve tiers plus a Contact-us Enterprise tier that resolve
consistently through both the public pricing page and the authenticated billing page,
**so that** I can pick and check out a plan (with a 14-day trial on Basic) without ever
hitting a broken/misrendered tier or a stale EUR price.

Purpose: This is the tracer per MVP mode — one thin end-to-end path proving the new
tier catalog flows through data, API, and UI consumers together. Plans 02-02 and 02-03
expand from this proven slice. Every subsequent plan reads from the new TIERS shape.

Output: Modified lib/plans.ts, app/api/stripe/checkout/route.ts, app/billing/page.tsx,
tests/plans.test.ts. Full pricing test suite (npx vitest run tests/plans.test.ts)
passes. Full test suite (npm test) passes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-marketing-pricing-surface/02-CONTEXT.md
@.planning/phases/02-marketing-pricing-surface/02-RESEARCH.md
@.planning/phases/02-marketing-pricing-surface/02-PATTERNS.md
@lib/plans.ts
@lib/billing.ts
@lib/usage.ts
@app/api/stripe/checkout/route.ts
@app/api/stripe/webhook/route.ts
@app/billing/page.tsx
@tests/plans.test.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to /api/stripe/checkout | Untrusted `tier` and `cadence` values arrive from the client; already validated against the TIERS catalog and CADENCES set before any Stripe API call |
| server to Stripe API | Server holds STRIPE_SECRET_KEY; only tier/cadence combinations with a configured price id can create a checkout session (missing env var already returns 503 in the existing route) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege / Tampering | app/api/stripe/checkout/route.ts validation block | high | mitigate | Extend the existing `if (!tier or tierKey === "free")` reject to ALSO reject tiers where `tier.contactSales === true`, so a crafted request `{tier: "pro"}` cannot self-serve-purchase Enterprise. Same 400 status and error string as the existing free-tier reject (Task 2, action a). |
| T-02-02 | Tampering (of pricing intent) | STRIPE_PRICE_* env vars pointing at stale EUR-priced Stripe Price objects after this phase deploys | high | mitigate | Deployment/ops mitigation, not code: the 9 new STRIPE_PRICE_{TIER}_{CADENCE} env vars are set as part of the phase-close human checkpoint (plan 02-04), superseding any old EUR-priced env vars. Stripe Price objects are immutable so the old ones must be replaced, not edited in place. |
</threat_model>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Rebuild the pricing catalog data shape (Basic/Growth/Premium/Enterprise, USD, contactSales) and update its test suite in one pass</name>
  <files>lib/plans.ts, tests/plans.test.ts</files>
  <read_first>
    - lib/plans.ts (full file, 132 lines — the single source of truth being modified)
    - tests/plans.test.ts (full file, 145 lines — assertions that must be reconciled with new field names and limits)
    - lib/usage.ts (lines 144-152 specifically — the effectiveTier() fallback that non-null-asserts getTier('free')!; do NOT remove the 'free' key)
    - app/api/stripe/webhook/route.ts (line 33 specifically — writes plan='free' on cancellation; confirms 'free' is a live resting state, not just display)
    - .planning/phases/02-marketing-pricing-surface/02-RESEARCH.md sections "Standard Stack" > "Recommended price points and limits", "Common Pitfalls" (all 5), "Assumptions Log" A1/A2, "Validation Architecture" > "Wave 0 Gaps"
    - .planning/phases/02-marketing-pricing-surface/02-PATTERNS.md section for `lib/plans.ts` (extend-in-place pattern, comment style)
  </read_first>
  <behavior>
    - Test 1: for every key in ["free","basic","growth","premium","pro"], getTier(key)?.key === key (PRICE-01 key preservation, Pitfall 1)
    - Test 2: TIERS.find(t => t.contactSales === true)?.key === "pro" AND its `.name` property === "Enterprise" (D-02)
    - Test 3: getTier("basic")!.monthlyUsd is within [1400, 1500] inclusive (PRICE-02, D-03)
    - Test 4: getTier("growth")!.monthlyUsd / getTier("basic")!.monthlyUsd is within [1.5, 2.0]; getTier("premium")!.monthlyUsd / getTier("growth")!.monthlyUsd is within [1.5, 2.0] (PRICE-02)
    - Test 5: displayPrice(getTier("basic")!, "monthly") === getTier("basic")!.monthlyUsd; displayPrice(getTier("basic")!, "yearly") === Math.round(getTier("basic")!.monthlyUsd * 12 * (1 - YEARLY_DISCOUNT)); displayPrice(getTier("basic")!, "weekly") === Math.round((getTier("basic")!.monthlyUsd / 4.33) * (1 + WEEKLY_PREMIUM)) (field rename per D-04)
    - Test 6: displayPrice(getTier("free")!, "monthly") === 0; displayPrice(getTier("pro")!, "monthly") === 0 (the contactSales branch returns 0 so the UI branch can pivot on contactSales, not on price zero alone — Pitfall 2)
    - Test 7: priceEnvVar("basic","weekly") === "STRIPE_PRICE_BASIC_WEEKLY"; priceEnvVar("growth","monthly") === "STRIPE_PRICE_GROWTH_MONTHLY"; priceEnvVar("premium","yearly") === "STRIPE_PRICE_PREMIUM_YEARLY" (PRICE-04 env-var contract — case sensitivity preserved)
    - Test 8 (NEW defensive test per RESEARCH Wave-0 gap): effectiveTier({ plan: p, subscription_status: "canceled" } as any) does NOT throw for p in ["free","basic","growth","premium","pro","trial","legacy-unknown-key"] (PRICE-05)
    - Test 9: The existing "outreach is a growth+ capability" test at lines 62-67 is UPDATED to match whichever outreach decision is applied to the new Basic tier (per RESEARCH Assumption A2: the recommendation is outreach true on Basic at 1450 USD, but either decision is defensible — pick one and keep the test consistent with the shipped TIERS entry)
  </behavior>
  <action>
    Extend lib/plans.ts in place per D-02/D-03/D-04 and Pitfall 1/2:
    (a) Keep the TierKey union unchanged (`"free" | "basic" | "growth" | "premium" | "pro"`) — do NOT rename `pro` to `enterprise`; changing the union widens the diff to every literal `"pro"` reference in webhook fallback, resolvePriceId legacy path, and tests (Alternatives table). Rename ONLY the display name.
    (b) Rename the Tier type's `monthlyEur` field to `monthlyUsd` (D-04) throughout: the type declaration, all 5 TIERS entries, and the displayPrice() function body (lines 116-127).
    (c) Add a new optional discriminator field to the Tier type: `contactSales?: boolean` (Pitfall 2). Do NOT reuse `monthlyUsd === 0` for Enterprise — that already means "Free" at 2 call sites.
    (d) Preserve the `free` tier entry unchanged in shape (Pitfall 1) — it remains the internal fallback/canceled-subscription resting state; only its display is removed by downstream plans. Keep its `monthlyUsd: 0`.
    (e) Reprice and relimit Basic/Growth/Premium per RESEARCH.md "Recommended price points and limits" (Basic 1450 USD; Growth 2500 USD with `featured: true`; Premium 4500 USD) or a defensible alternative within PRICE-02's [1400,1500] base and [1.5,2.0] step-up ranges. Set the corresponding new TierLimits values (events/waves/suppliers/seats/outreach/export/maxEventSpendUsd) — following RESEARCH's recommendation or a documented deviation.
    (f) Retire `pro` as a self-serve tier: rename its .name from "Pro" to "Enterprise", set `contactSales: true`, keep its monthlyUsd 0 (unused when contactSales is true), keep its `key: "pro"` unchanged, and update its blurb to a Contact-us framing.
    (g) In displayPrice(), add a leading branch: if `tier.contactSales === true` return 0 (per Test 6). This lets consumers pivot on the contactSales flag while numeric zero remains an unambiguous "no numeric price" signal.
    (h) Preserve the file's existing long block-comment style — add a short rationale comment above the Enterprise entry noting the contactSales-versus-zero-price rationale (Pitfall 2 in 02-RESEARCH.md) and above the outreach setting on the Basic tier documenting the chosen policy (Assumption A2).
    Update tests/plans.test.ts in the SAME change (Pitfall 5 — do NOT defer to verification time):
    - Line 15's for-loop over keys keeps the same key set (Test 1); no structural edit but re-verify after data change.
    - Lines 62-67's outreach-gating assertions (Test 9): reconcile with whichever basic.limits.outreach value is shipped in step (e).
    - Lines 69-78's Growth limit assertions: update the exact numbers to whatever Growth's new limits are set to in step (e).
    - Lines 113, 118, 142's .monthlyEur field references: rename to .monthlyUsd throughout.
    - Add the price-range assertion (Test 3), the step-up-ratio assertion (Test 4), the contactSales discovery assertion (Test 2), and the displayPrice-with-contactSales assertion (Test 6) as new it(...) blocks in the existing describe structure.
    - Add the effectiveTier defensive regression test (Test 8) — this may live in tests/plans.test.ts as a new describe block or a new file tests/billing-defensive.test.ts per RESEARCH Wave 0 Gaps; either location is acceptable. Import effectiveTier from @/lib/usage.
    NEVER remove the `free` entry from TIERS, NEVER rename the TierKey union, NEVER set contactSales true alongside a non-zero monthlyUsd (they are mutually exclusive by convention — Pitfall 2).
  </action>
  <verify>
    <automated>npx vitest run tests/plans.test.ts 2>&1 && grep -v '^\s*//' lib/plans.ts | grep -v '^\s*\*' | grep -c 'contactSales' && test $(grep -v '^\s*//' lib/plans.ts | grep -v '^\s*\*' | grep -cE '\bmonthlyEur\b') -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run tests/plans.test.ts exits 0 with all existing plus new tests green
    - Non-comment occurrences of `contactSales` in lib/plans.ts >= 2 (the type declaration plus the Enterprise entry, at minimum)
    - Non-comment occurrences of `monthlyEur` in lib/plans.ts === 0 (field fully renamed)
    - Non-comment occurrences of `.monthlyEur` in tests/plans.test.ts === 0 (test field references renamed)
    - `key: "free"` still appears in TIERS at least once (free key preserved, Pitfall 1)
    - TierKey union still contains the "free" literal — verified by successful TypeScript compilation in the umbrella verify (plan 02-04)
    - The new tier ordering (Basic monthlyUsd less than Growth monthlyUsd less than Premium monthlyUsd) is asserted by the step-up ratio test
    - No test file outside tests/plans.test.ts (or the new tests/billing-defensive.test.ts) is modified
  </acceptance_criteria>
  <done>lib/plans.ts describes the new USD Basic/Growth/Premium/Enterprise catalog with contactSales discrimination and preserved 'free' key; tests/plans.test.ts (and optionally tests/billing-defensive.test.ts) fully covers the new shape including the PRICE-05 defensive regression; npx vitest run tests/plans.test.ts passes clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Basic-tier trial and Enterprise-reject into the Stripe checkout route</name>
  <files>app/api/stripe/checkout/route.ts</files>
  <read_first>
    - app/api/stripe/checkout/route.ts (full file, 107 lines — extending in place)
    - lib/plans.ts (post-Task-1 state — read the new Tier.contactSales field)
    - .planning/phases/02-marketing-pricing-surface/02-RESEARCH.md sections "Architecture Patterns" > "Pattern 2: Stripe Checkout trial config", "Assumptions Log" A3 (verify field nesting against Stripe TypeScript types), "Security Domain" (threat: crafted request for contactSales tier)
    - .planning/phases/02-marketing-pricing-surface/02-PATTERNS.md section for app/api/stripe/checkout/route.ts (validation-before-branch, error-response shape, auth/role pattern)
    - node_modules/stripe/types/checkout/SessionsResource.d.ts (or equivalent) — spot-check Stripe.Checkout.SessionCreateParams to confirm trial_period_days lives under subscription_data and payment_method_collection is a top-level Session field, per Assumption A3
  </read_first>
  <behavior>
    - Test 1 (integration/route-level, may be manual invocation or a new unit test): POST /api/stripe/checkout with body { tier: "pro", cadence: "monthly" } (authenticated as an admin org member) returns HTTP 400 with JSON { error: "Invalid plan selected" } — Enterprise/contactSales tier is not self-serve checkoutable (Security threat T-02-01)
    - Test 2: POST with { tier: "free", cadence: "monthly" } still returns HTTP 400 { error: "Invalid plan selected" } (pre-existing behavior preserved)
    - Test 3: When the route calls stripe.checkout.sessions.create(...) for tierKey === "basic", the subscription_data.trial_period_days field is 14 in the passed params; for tierKey in { "growth", "premium" } the field is absent — the ONLY tier gated for the Stripe-side trial is Basic (PRICE-03, D-05)
    - Test 4: The existing admin-role check (requireRole(ctx, "admin")) and the existing Unauthorized / Invalid billing cadence error paths continue to behave unchanged (regression preservation)
  </behavior>
  <action>
    Extend app/api/stripe/checkout/route.ts in place, preserving the file's existing validation-before-branch ordering:
    (a) In the validation block currently at lines 38-44 (`if (!tier || tierKey === "free") { return NextResponse.json({ error: "Invalid plan selected" }, { status: 400 }); }`), extend the condition to ALSO reject contactSales tiers: `if (!tier || tierKey === "free" || tier.contactSales === true)`. Same status code (400), same error string ("Invalid plan selected") — matches the existing style precisely; do NOT introduce a new error message that would break existing UI error-copy assumptions. This is the mitigation for threat T-02-01.
    (b) In createSession() (currently at lines 70-83), extend the subscription_data object to conditionally include `trial_period_days: 14` only when `tierKey === "basic"` — merge into a SINGLE subscription_data object (do not spread twice). The pattern is a single spread of `{ trial_period_days: 14 }` guarded by the tierKey check inside the same subscription_data literal, alongside the existing metadata property.
    (c) Optional (Claude's discretion per D-05): for the Basic tier, ALSO set top-level `payment_method_collection: "if_required"` and `subscription_data.trial_settings: { end_behavior: { missing_payment_method: "cancel" } }` to match the existing 14-day free trial no-credit-card-required copy from app/page.tsx's jsonLd. If added, payment_method_collection is a TOP-LEVEL Session field, NOT nested under subscription_data (per Assumption A3, verify against Stripe TS types before merge). If Stripe's TS types reject the nesting/values chosen, remove these two optional fields — trial_period_days alone is the required minimum for PRICE-03.
    (d) Preserve the existing outer try/catch and stale-customer inner-catch shape (lines 85-106); do NOT restructure error handling.
    (e) Preserve the existing requireRole(ctx, "admin") gate (lines 14-18) and the CADENCES.has(cadence) cadence-validation gate (lines 42-44) unchanged — the new trial branch runs AFTER validation, never before.
    NEVER set trial_period_days for any tier other than Basic (would violate PRICE-03). NEVER remove the pre-existing tierKey === "free" rejection.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -cE 'checkout/route\.ts' | grep -q '^0$' && grep -v '^\s*//' app/api/stripe/checkout/route.ts | grep -cE 'tier\.contactSales' && grep -v '^\s*//' app/api/stripe/checkout/route.ts | grep -cE 'trial_period_days:\s*14' && grep -v '^\s*//' app/api/stripe/checkout/route.ts | grep -cE 'tierKey\s*===\s*"basic"'</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0 (no TypeScript errors introduced against Stripe.Checkout.SessionCreateParams types)
    - Non-comment occurrences of `tier.contactSales` in app/api/stripe/checkout/route.ts >= 1 (the extended validation branch)
    - Non-comment occurrences of `trial_period_days: 14` in app/api/stripe/checkout/route.ts === 1 (single site, inside createSession)
    - Non-comment occurrences of `tierKey === "basic"` in app/api/stripe/checkout/route.ts >= 1 (the trial-gating discriminator)
    - The pre-existing `if (!tier || tierKey === "free")` reject condition is still present, extended (not replaced) with the contactSales clause
    - Zero changes to lines 14-18 (auth/role gate) and lines 85-106 (error handling shape)
  </acceptance_criteria>
  <done>Enterprise tier cannot be self-serve-checked-out (400 Invalid plan selected); Basic tier's Stripe checkout session includes trial_period_days: 14; Growth and Premium sessions do not; TypeScript compiles clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update the authenticated billing page to render USD, Contact-sales branch, and filter the internal 'free' tier</name>
  <files>app/billing/page.tsx</files>
  <read_first>
    - app/billing/page.tsx (full file, 417 lines — the tier comparison grid and per-tier price rendering)
    - lib/plans.ts (post-Task-1 state — read Tier.contactSales, Tier.monthlyUsd, and the retained `free` key)
    - .planning/phases/02-marketing-pricing-surface/02-RESEARCH.md section "Pitfall 2" (price zero collides with Free branch), "Pattern 2 code example", and "Open Questions" Q2 (whether to filter `free` from the in-app grid — recommendation: yes)
    - .planning/phases/02-marketing-pricing-surface/02-PATTERNS.md section for `app/billing/page.tsx` (price-zero-means-Free branch rewrite pattern, TIERS.filter(t => t.key !== 'free') pattern)
    - .planning/phases/02-marketing-pricing-surface/02-UI-SPEC.md section "Copywriting Contract" (rows: Enterprise tier CTA "Contact sales" -> mailto:hello@sourcegpt.org, Growth CTA "Choose Growth", Error state — tier temporarily unavailable "Coming soon")
    - lib/legal.ts (source of COMPANY.contactEmail for the Contact sales mailto)
  </read_first>
  <behavior>
    - Test 1 (visual/manual — no rendering test infra exists per RESEARCH Wave-0 gap, verified in plan 02-04's human checkpoint): The comparison grid shows exactly 4 columns — Basic, Growth, Premium, Enterprise — with the internal `free` tier filtered out (Open Question 2 recommendation adopted)
    - Test 2 (visual): The Enterprise column shows a "Contact sales" CTA linking to mailto:hello@sourcegpt.org (or COMPANY.contactEmail), NOT the "Free" label or the "No card required" copy; verified because contactSales is checked BEFORE the price === 0 branch (Pitfall 2)
    - Test 3 (visual): Every price appears with the "$" symbol using tier.monthlyUsd (renamed from monthlyEur) — no lingering "€" symbol on this page
    - Test 4: TypeScript compiles clean against the renamed Tier.monthlyUsd field (compile-time catch for any missed field-reference rename)
  </behavior>
  <action>
    Extend app/billing/page.tsx in place:
    (a) Add a TIERS.filter step before .map(...) in the comparison grid: filter out `t => t.key === "free"` so the internal fallback tier is not rendered in the authenticated UI (Open Question 2 recommendation — the `free` column reads as a UI regression next to enterprise-tier pricing).
    (b) Rewrite the price-rendering branch currently at lines 370-377 so contactSales is checked FIRST: if `tier.contactSales === true` render "Contact sales" (per UI-SPEC Copywriting Contract), else if `price === 0` fall into the existing "Free" branch (which now only fires for the filtered-out `free` tier, so effectively dead on this page — but keep it for defensive correctness), else render `$` + `price.toLocaleString()` using the renamed monthlyUsd-derived value.
    (c) Rewrite the "No card required" copy branch currently at lines 403-404 with the same discriminator-first ordering: `tier.contactSales === true` renders "Contact sales" CTA linking to `mailto:${COMPANY.contactEmail}` (import COMPANY from @/lib/legal per the mailto pattern already used in app/legal/privacy/page.tsx:11); `tier.key === "free"` renders "No card required" (defensive only); else the normal "Choose {tier.name}" CTA that already exists.
    (d) Rename every `.monthlyEur` reference on the page to `.monthlyUsd`; replace every literal "€" symbol used for price display with "$" (search-and-replace scoped to this file only — do NOT rename the constants YEARLY_DISCOUNT etc.). Preserve every `t(...)` i18n wrapping — never hardcode a raw string that was previously t()-wrapped.
    (e) Preserve every other aspect of the page unchanged — the redirect handling, the "Coming soon" fallback for missing STRIPE_PRICE env vars (existing pattern in the disabled-CTA state), the auth/session flow, and any error-boundary copy.
    NEVER re-order the discriminator check so that price === 0 is checked before contactSales (Pitfall 2 — would render Enterprise as "Free"). NEVER add a new tier column or renderer beyond the four self-serve targets plus Enterprise.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -cE 'billing/page\.ts' | grep -q '^0$' && grep -v '^\s*//' app/billing/page.tsx | grep -cE 'contactSales' && grep -v '^\s*//' app/billing/page.tsx | grep -cE 'monthlyUsd' && test $(grep -v '^\s*//' app/billing/page.tsx | grep -cE '\bmonthlyEur\b') -eq 0 && grep -v '^\s*//' app/billing/page.tsx | grep -cE 'filter\(.*key.*!==.*"free"'</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0 for app/billing/page.tsx (no TypeScript errors on the field rename)
    - Non-comment occurrences of `contactSales` in app/billing/page.tsx >= 2 (the two rewritten discriminator branches, price display and CTA)
    - Non-comment occurrences of `monthlyUsd` in app/billing/page.tsx >= 1
    - Non-comment occurrences of `monthlyEur` in app/billing/page.tsx === 0
    - A filter expression removing the `free` tier from the comparison grid is present
    - The Enterprise "Contact sales" CTA links to a mailto: URL sourced from COMPANY.contactEmail
    - t() i18n-wrapping is preserved on every user-facing string that was previously wrapped
  </acceptance_criteria>
  <done>Authenticated billing page renders the new 4-tier USD grid with a Contact-sales branch for Enterprise, no `free` column, no lingering "€" symbol; TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
Automated per-task verify commands above. Cross-task verification for this plan:
- `npm test` passes (full suite, not just plans.test.ts, to catch downstream regressions on lib/billing.ts's tier resolution)
- `npx tsc --noEmit` exits 0 (full-project typecheck catches any missed monthlyEur reference in files not listed here)
Full phase verification (typecheck + lint + test + build + human visual checks) runs in plan 02-04.
</verification>

<success_criteria>
- The new Basic/Growth/Premium/Enterprise catalog is the single source of truth in lib/plans.ts, with the `free` key preserved as internal fallback
- POST /api/stripe/checkout rejects `{tier: "pro"}` and `{tier: "free"}` with 400 Invalid plan selected; accepts basic/growth/premium given a configured price env var; sets trial_period_days: 14 only for basic
- /billing page displays 4 tier columns (no `free`), USD prices, Contact-sales CTA for Enterprise
- All tier-catalog tests pass with the new field name and shape
</success_criteria>

<output>
Create `.planning/phases/02-marketing-pricing-surface/02-01-SUMMARY.md` when done, documenting: exact monthlyUsd values chosen for Basic/Growth/Premium, chosen basic.limits.outreach value (with rationale per A2), any deviation from the recommended payment_method_collection / trial_settings values chosen in Task 2.
</output>
