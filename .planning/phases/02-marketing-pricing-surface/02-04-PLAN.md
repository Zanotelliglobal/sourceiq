---
phase: 02-marketing-pricing-surface
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified: []
autonomous: false
requirements: [PRICE-01, PRICE-02, PRICE-03, PRICE-04, PRICE-05, MKT-01, MKT-02, MKT-03, MKT-04, MKT-05]

estimate:
  tokens: 25000
  raw_tokens: 20000
  tasks: 3
  confidence: low

user_setup:
  - service: stripe
    why: "Create 9 new Stripe Price objects (Basic/Growth/Premium × weekly/monthly/yearly) and set the matching STRIPE_PRICE_<TIER>_<CADENCE> env vars — this is a live-account action that cannot be scripted by the execution agent (see RESEARCH Environment Availability)"
    env_vars:
      - name: STRIPE_PRICE_BASIC_WEEKLY
        source: "Stripe Dashboard → Products → SourceGPT Basic → Add price (weekly recurring, USD, matches the weekly displayPrice for the shipped basic.monthlyUsd from plan 02-01)"
      - name: STRIPE_PRICE_BASIC_MONTHLY
        source: "Stripe Dashboard → Products → SourceGPT Basic → Add price (monthly recurring, USD, matches basic.monthlyUsd from plan 02-01)"
      - name: STRIPE_PRICE_BASIC_YEARLY
        source: "Stripe Dashboard → Products → SourceGPT Basic → Add price (yearly recurring, USD, matches yearly displayPrice for the shipped basic.monthlyUsd from plan 02-01)"
      - name: STRIPE_PRICE_GROWTH_WEEKLY
        source: "Stripe Dashboard → Products → SourceGPT Growth (as above, growth prices)"
      - name: STRIPE_PRICE_GROWTH_MONTHLY
        source: "Stripe Dashboard → Products → SourceGPT Growth"
      - name: STRIPE_PRICE_GROWTH_YEARLY
        source: "Stripe Dashboard → Products → SourceGPT Growth"
      - name: STRIPE_PRICE_PREMIUM_WEEKLY
        source: "Stripe Dashboard → Products → SourceGPT Premium (as above)"
      - name: STRIPE_PRICE_PREMIUM_MONTHLY
        source: "Stripe Dashboard → Products → SourceGPT Premium"
      - name: STRIPE_PRICE_PREMIUM_YEARLY
        source: "Stripe Dashboard → Products → SourceGPT Premium"
    dashboard_config:
      - task: "Unset (or archive) the pre-existing STRIPE_PRICE_* env vars that pointed at the old EUR-priced Stripe Price objects (threat T-02-02: stale-EUR-price mitigation). Stripe Price objects are immutable, so old ones must be superseded, not edited."
        location: "Stripe Dashboard → Products (archive old prices) + your deployment platform's env-var settings (unset the pre-Phase-2 STRIPE_PRICE_* vars if their values were repointed)"
      - task: "Confirm Stripe Checkout allows the Basic-tier trial_period_days: 14 configuration set in plan 02-01 task 2 (should be default; only relevant if account has a global no-trials policy toggle set)"
        location: "Stripe Dashboard → Settings → Billing → Subscriptions and trial settings"

must_haves:
  truths:
    - "As a maintainer, I want the phase to reach a green verification suite (typecheck + lint + test + build) and a human sign-off on the visual/interactive checks that no automated test infra covers, so that Phase 2 is provably shipped before I close it (D-11)"
    - "npm run typecheck exits 0"
    - "npm run lint exits 0"
    - "npm test exits 0 (full Vitest suite, including all pre-Phase-2 tests still green plus the tests/plans.test.ts additions from plan 02-01)"
    - "npm run build exits 0 (Next.js build succeeds; this is the compile-time catch for Pitfall 3 lucide-react brand-icon imports and any missed field rename)"
    - "A repo-wide grep for `monthlyEur` in non-comment source lines outside .planning/ returns zero hits (D-04 field rename fully complete)"
    - "A repo-wide grep for lucide-react imports of Facebook / Instagram / Linkedin returns zero hits (Pitfall 3 confirmed absent — the SocialIcons.tsx workaround is the only path)"
    - "A repo-wide grep for `<footer` in components/LandingContent.tsx returns zero hits (the pre-existing duplicate inline footer is fully deleted per plan 02-02 task 3)"
    - "The 9 new STRIPE_PRICE_<TIER>_<CADENCE> env vars are set in the deployment environment (verified by the human checkpoint — the execution agent cannot access Stripe Dashboard or production env-var settings from the sandbox)"
    - "The rendered /billing page shows 4 tier columns (no `free` column), USD prices, and a Contact-sales CTA on Enterprise — verified by human visual check"
    - "The rendered / (landing) page shows 4 pricing tier cards, 6-tile feature grid, closing CTA banner, Hero media slot with the placeholder SVG, and exactly ONE footer (SiteFooter, not the deleted inline duplicate) — verified by human visual check"
    - "The rendered /legal/ccpa page shows a real CCPA policy with real content (not placeholder), reached from the SiteFooter CCPA link — verified by human visual check"
    - "The Basic-tier 'Start free trial' CTA on the landing page pricing card links to /sign-up; no other pricing card is marketed as a trial entry point — verified by human visual check (PRICE-03)"
  artifacts:
    - "No code artifacts from this plan — verification-only + human checkpoints"
  key_links:
    - "The 9 Stripe Price objects (PRICE-04) MUST exist in the Stripe account and their IDs MUST be set in the deployment env before /billing checkout works for the new tiers — this cannot be done by the execution agent; the human checkpoint is the only path to closing PRICE-04"
    - "Old EUR STRIPE_PRICE_* env vars (if any) MUST be unset/repointed at the SAME time the new ones are set, closing threat T-02-02 (stale-EUR-price checkout window). Doing one without the other leaves either a broken checkout (new prices missing) or a stale-price checkout (old prices still winning). Both actions are part of the same human step."
    - "The visual/interactive checks in Task 2 are the SOLE verification for MKT-01 / MKT-02 / MKT-03 / MKT-04 / MKT-05 acceptance criteria — the repo has no automated component-rendering test infra (RESEARCH Wave 0 Gaps), so a green typecheck/lint/test/build alone does NOT prove these requirements shipped correctly"

artifacts_produced:
  - "Phase-2 completion sign-off in STATE.md and ROADMAP.md (verified end-of-phase gate satisfied)"
---

<objective>
Close out Phase 2 with (a) the full standing verification suite green, (b) a human
checkpoint creating the 9 new Stripe Price objects and setting the matching env vars
that only a live Stripe account holder can do, and (c) a human visual/interactive
check of all the marketing surface changes plans 02-02 and 02-03 shipped — no
automated component-rendering test infrastructure exists in this repo (RESEARCH Wave 0
Gaps), so the visual checks are the only verification path for MKT-01/02/03/04/05.

Purpose: This is the phase gate. All requirements PRICE-01..05 and MKT-01..05 land
against these three tasks: verify passes, prices are live, humans confirm the visual
surface matches the spec.

Output: Phase-2 sign-off; user has verified pricing/marketing surface end-to-end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-marketing-pricing-surface/02-CONTEXT.md
@.planning/phases/02-marketing-pricing-surface/02-RESEARCH.md
@.planning/phases/02-marketing-pricing-surface/02-UI-SPEC.md
@.planning/phases/02-marketing-pricing-surface/02-01-PLAN.md
@.planning/phases/02-marketing-pricing-surface/02-02-PLAN.md
@.planning/phases/02-marketing-pricing-surface/02-03-PLAN.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| execution agent -> Stripe live account | The agent's sandbox has no STRIPE_SECRET_KEY access and no dashboard credentials; the human checkpoint is the only path to creating live Price objects |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 (carried from 02-01) | Tampering (of pricing intent) | Stale-EUR Stripe Price env vars vs. new USD Price env vars | high | mitigate | Handled by Task 1 human checkpoint: setting the new env vars and unsetting/repointing the old ones happen in the same maintenance window |
| T-02-07 | Repudiation | Phase seals without live Stripe Prices → PRICE-04 silently unshipped in production | high | mitigate | Task 1 is a `checkpoint:human-verify` blocking gate — the phase cannot autonomously mark PRICE-04 done without the human confirming the 9 env vars are set |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Run the full verification suite after all Phase 2 plans (02-01, 02-02, 02-03) have landed</name>
  <files>none (verification only)</files>
  <read_first>
    - package.json scripts block (confirm typecheck/lint/test/build commands are unchanged from Phase 1)
    - .planning/phases/02-marketing-pricing-surface/02-01-PLAN.md, 02-02-PLAN.md, 02-03-PLAN.md — confirm all three are in `[x]` state (all tasks complete, all SUMMARY.mds written) before running verification
  </read_first>
  <action>
    Run the project's standing full verification command, in order: `npm run typecheck`, `npm run lint`, `npm test`, `npm run build` (per D-11), confirming each exits 0 now that Plans 02-01 (catalog + checkout + billing + tests), 02-02 (LandingContent overhaul + Hero SVG asset), and 02-03 (SiteFooter + SocialIcons + CCPA page) have all landed. This is the first point where every file touched across all three prior plans is checked together in one pass.
    ALSO run repo-wide sanity greps that the field rename and duplicate-footer deletion are complete:
    - `git grep -nE '\bmonthlyEur\b' -- ':(exclude).planning' ':(exclude)node_modules' ':(exclude).claude'` — MUST return zero hits (D-04 field rename complete outside planning docs)
    - `git grep -nE 'from "lucide-react".*\b(Facebook|Instagram|Linkedin)\b' -- ':(exclude).planning'` — MUST return zero hits (Pitfall 3 absent)
    - `git grep -nE '^\s*<footer\b' components/LandingContent.tsx` — MUST return zero hits (inline duplicate deleted per plan 02-02 task 3)
    - `git grep -nE '"/legal/ccpa"' components/SiteFooter.tsx` — MUST return exactly 1 hit (CCPA link added per plan 02-03 task 2)
    If any check fails, STOP and route to the relevant plan for re-execution (do NOT patch here — plans are the unit of correction).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm test && npm run build && test $(git grep -nE '\bmonthlyEur\b' -- ':(exclude).planning' ':(exclude)node_modules' ':(exclude).claude' 2>/dev/null | wc -l | tr -d ' ') -eq 0 && test $(git grep -nE 'from "lucide-react".*\b(Facebook|Instagram|Linkedin)\b' -- ':(exclude).planning' 2>/dev/null | wc -l | tr -d ' ') -eq 0 && test $(git grep -nE '^\s*<footer\b' components/LandingContent.tsx 2>/dev/null | wc -l | tr -d ' ') -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - npm run typecheck exits 0
    - npm run lint exits 0
    - npm test exits 0 (all tests, including the updated tests/plans.test.ts and any new tests/billing-defensive.test.ts)
    - npm run build exits 0 (Next.js build succeeds; CCPA route discoverable in the build output; no lucide-react brand-icon compile error)
    - The 3 sanity greps above each return zero hits (or exactly 1 hit for the CCPA link presence check)
  </acceptance_criteria>
  <done>Automated verification suite green across all four commands and all sanity-grep checks. Phase 2 is code-complete pending only the Stripe Prices human checkpoint and the visual human checkpoint below.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human checkpoint — create the 9 new Stripe Price objects and set the STRIPE_PRICE_* env vars</name>
  <what-built>Plan 02-01 wired `lib/plans.ts` to resolve `STRIPE_PRICE_<TIER>_<CADENCE>` env vars for each self-serve tier × cadence combination, and gated Basic-tier checkout on `trial_period_days: 14`. The execution agent CANNOT create the actual Stripe Price objects (no live-account access from the sandbox) — that is your action. Once done, `/billing` checkout for Basic/Growth/Premium at any cadence will resolve a real price id and complete a Stripe Checkout Session; until done, those buttons show "Coming soon" per the existing missing-price fallback pattern in app/billing/page.tsx (RESEARCH Environment Availability).</what-built>
  <how-to-verify>
    Perform the following in your Stripe Dashboard (production and/or test mode as appropriate):
    1. Go to Products → find or create three Product objects: "SourceGPT Basic", "SourceGPT Growth", "SourceGPT Premium".
    2. For each Product, add 3 recurring prices in USD (currency: usd):
        - Weekly: amount = displayPrice(tier, "weekly") from lib/plans.ts (round to cents; e.g. for basic.monthlyUsd=1450, weekly = Math.round(1450/4.33 * 1.25) = 419 USD → set 41900 cents)
        - Monthly: amount = tier.monthlyUsd (e.g. 1450 USD → 145000 cents)
        - Yearly: amount = displayPrice(tier, "yearly") (e.g. Math.round(1450 * 12 * 0.8) = 13920 USD → 1392000 cents)
       Use whatever monthlyUsd values plan 02-01 actually shipped (documented in 02-01-SUMMARY.md), NOT the recommended values shown here — verify against the shipped catalog.
    3. Copy each new Price id (starts with `price_`) into your deployment environment as the matching STRIPE_PRICE_<TIER>_<CADENCE> env var (9 total). For local development this is .env.local; for production this is your platform's env-var settings (Vercel dashboard or equivalent).
    4. If any pre-Phase-2 STRIPE_PRICE_* env vars remain pointing at the OLD EUR-priced Price objects, unset them at the same time — threat T-02-02 requires no stale-price window. If you're not sure whether any exist, list all env vars matching STRIPE_PRICE_* and confirm they now all point at the new USD Price ids.
    5. In the Stripe Dashboard, confirm that Subscription trial settings (Settings → Billing → Subscriptions) don't have a global no-trials policy that would block the Basic-tier `trial_period_days: 14` from plan 02-01 task 2. Default is fine; only flag if a custom policy is set.
    6. In a running dev server (npm run dev), visit /billing while signed in as an admin org member. Confirm each of Basic/Growth/Premium at each cadence shows an active "Choose Basic/Growth/Premium" button (not the "Coming soon" fallback), and that clicking Basic → Choose triggers a Stripe Checkout Session with a visible 14-day trial period. Enterprise should show a Contact-sales CTA, not a checkout button. `free` tier should not appear as a column.
    7. Attempt (from a browser devtools console or curl) to POST /api/stripe/checkout with body `{ tier: "pro", cadence: "monthly" }` — verify it returns 400 with `error: "Invalid plan selected"` (threat T-02-01 mitigation from plan 02-01 task 2 confirmed live).
  </how-to-verify>
  <acceptance_criteria>
    - 9 new Stripe Price objects exist in the Stripe account (Basic/Growth/Premium × weekly/monthly/yearly), USD-denominated, values matching displayPrice() derivations for the shipped monthlyUsd
    - 9 STRIPE_PRICE_<TIER>_<CADENCE> env vars are set in the deployment environment(s) pointing at the new Price ids
    - Any pre-existing STRIPE_PRICE_* env vars pointing at old EUR prices are unset or repointed
    - /billing renders each self-serve tier with an active checkout button; Basic checkout initiates a 14-day trial; Enterprise shows Contact-sales; `free` tier is not rendered as a column
    - POST /api/stripe/checkout with `{tier: "pro"}` returns 400
  </acceptance_criteria>
  <resume-signal>Type "approved" once the 9 Price objects are created, the env vars are set, the old EUR env vars (if any) are unset, and the /billing spot-check confirms each tier flows through. Or describe any issues encountered (e.g. Stripe Dashboard errors, unexpected UI behavior, missing tiers) so plans 02-01/02-03 can be re-opened for repair.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual/interactive check of the full marketing surface</name>
  <what-built>Plans 02-02 and 02-03 shipped all the user-visible marketing surface changes: 4-tier public pricing cards, 6-tile feature grid, closing CTA banner, Hero media slot, extended global SiteFooter (logo/tagline/contact/social/legal/copyright), new CCPA page. The repo has no automated component-rendering test infrastructure (RESEARCH Wave 0 Gaps), so this human visual check is the sole verification path for MKT-01, MKT-02, MKT-03, MKT-04, MKT-05 and for the visual halves of PRICE-01, PRICE-03.</what-built>
  <how-to-verify>
    In a running dev server (`npm run dev`), visit each URL below and confirm the behavior/appearance matches the spec.

    **URL: /  (landing page, signed OUT)**
    1. Hero section: shows an <img> preview referencing /hero-placeholder.svg (the placeholder SVG committed by plan 02-02). The "Product preview" caption is visible. The hero displays this image, not the pre-existing hardcoded fake-data mock. (MKT-05, Pitfall 4)
    2. Feature grid section: exactly 6 tiles (per D-06 canonical mapping in docs/change-request-backlog.md §6) between "How it works" and "Pricing", in this order: Autonomous Sourcing Engine, Compliance & Audit, Workflow Automation, Collaboration Hub, Budget & Spend Intelligence, Supplier Marketplace (repositioned copy "AI discovers and verifies suppliers live from the web."). NO RFP & Intake Tools tile. NO ERP/API Integrations tile. Tile titles are 16px semibold, tile bodies are 16px regular — not bold. (MKT-04)
    3. Pricing section: exactly 4 cards — Basic, Growth (with "Most popular" amber badge), Premium, Enterprise. NO Free/Trial card. USD prices shown ("$" symbol, not "€"). Cadence toggle (weekly/monthly/yearly) still works and updates all 4 cards. (PRICE-01, PRICE-03)
    4. Basic card CTA: "Start free trial" → links to /sign-up. This is the ONLY card marketed as a trial entry point. (PRICE-03)
    5. Growth/Premium card CTAs: "Choose Growth" / "Choose Premium" → each links to /sign-up or /billing per the existing pattern.
    6. Enterprise card: "Contact sales" CTA linking to mailto:hello@sourcegpt.org. No $ price displayed; no "Free" label. (Pitfall 2 confirmed live)
    7. Final CTA section (pre-existing): still present, unchanged.
    8. Closing CTA banner (NEW): "Don't see a perfect fit?" heading + body copy + amber "Talk to sales" button linking to mailto:hello@sourcegpt.org. Distinct visual treatment from the Final CTA above it. (MKT-01)
    9. Footer at the bottom: exactly ONE footer (the extended SiteFooter, not the deleted inline duplicate). Contains Sparkles logo + "SourceGPT" wordmark, tagline "AI-powered supplier intelligence.", contact mailto (visible email address), 3 social icons (Facebook/Instagram/LinkedIn) as icon-only links with visible hover states, 3 legal links (Privacy Policy / Terms of Service / CCPA Policy), copyright line. (MKT-02, RESEARCH Pattern 3 — duplicate-footer bug fixed)
    10. Resize the browser to 375px width (mobile). Confirm: footer row wraps rather than clips or horizontal-scrolls; pricing cards stack vertically; feature-grid tiles stack; Hero image scales. (UI-SPEC UI Considerations overflow)

    **URL: /legal/ccpa (NEW route)**
    1. Page renders (200, not 404). Title bar reads "CCPA Policy — SourceGPT". (MKT-03)
    2. Uses the same LegalLayout template as /legal/privacy and /legal/terms (visual parity — same layout, back link to /dashboard visible at the top).
    3. Content is real: sections on categories of data collected, CCPA rights (know/delete/opt-out/non-discrimination), and how-to-exercise (with contact mailto). NOT lorem ipsum or a placeholder stub. (D-07)
    4. Does NOT claim formal legal-counsel review (prohibition T-02-05 mitigation).

    **URL: /billing (signed IN as admin)**
    1. Tier comparison grid shows exactly 4 columns — Basic, Growth, Premium, Enterprise. NO Free column. (Open Question 2 recommendation adopted)
    2. Prices in USD; no lingering "€" symbol anywhere on the page.
    3. Enterprise column shows "Contact sales" CTA; NOT "Free" / "No card required". (Pitfall 2 confirmed live)
    4. Clicking "Choose Basic" initiates a Stripe Checkout Session with a 14-day trial period (verified again in Task 2 above; confirming here that the /billing UI actually surfaces the working button).

    **URL: /dashboard, /events, /events/{id} (any authenticated page)**
    1. Footer at the bottom shows the extended SiteFooter — same content as on / — because it's rendered globally by app/layout.tsx. (MKT-02 site-wide coverage)

    **URL: /legal/privacy and /legal/terms (regression check)**
    1. Pages still render unchanged from Phase 1. Their content is not affected by this phase; a Privacy/Terms regression means an accidental edit crept in and needs investigation.
  </how-to-verify>
  <acceptance_criteria>
    - Every URL above renders as described; no 404s, no obvious visual regressions
    - MKT-01 (closing CTA banner), MKT-02 (footer content), MKT-03 (CCPA page), MKT-04 (feature-grid copy matching resolved backlog mapping), MKT-05 (hero media slot) all confirmed visually
    - PRICE-01 (4 tiers no free), PRICE-03 (Basic-only trial) confirmed visually
    - Pitfalls 2 (Free-vs-Contact-sales) and 4 (hero media slot vs fake mock) confirmed absent from the shipped UI
    - Mobile viewport (375px) does not clip/scroll the footer or break any section layout
  </acceptance_criteria>
  <resume-signal>Type "approved" if every URL renders as described and every MKT/PRICE acceptance criterion above is visually confirmed. Or describe specific issues (e.g. "Growth card's badge is not amber", "CCPA page missing rights section", "footer wraps but overlaps") so the relevant plan can be re-opened for repair.</resume-signal>
</task>

</tasks>

<verification>
Automated verification is Task 1; human verification is Tasks 2 and 3. This plan does not modify code — it verifies and gates.
</verification>

<success_criteria>
- Full verification suite (typecheck + lint + test + build) passes with all Phase 2 plans landed
- 9 new Stripe Prices exist in the account and their ids are set as env vars; any stale EUR env vars are unset
- Human visual sign-off on the full landing page, /billing, /legal/ccpa, and site-wide SiteFooter presence
- Phase 2 marked complete in ROADMAP.md and STATE.md
</success_criteria>

<output>
Create `.planning/phases/02-marketing-pricing-surface/02-04-SUMMARY.md` when done, documenting: which Stripe Prices were created (values + Price ids optional, but at minimum "9 new prices set, all matching shipped catalog"), any human-observed visual issues that were fixed before sign-off, and Phase 2 close date.

Also update:
- .planning/STATE.md — mark Phase 2 complete, decrement remaining phases, log the phase close date
- .planning/ROADMAP.md — mark Phase 2's `- [ ]` box to `- [x]`, update Plans: line to "4/4 plans executed", mark each 02-NN-PLAN.md line as `- [x]`
</output>
