---
phase: 03-persistent-supplier-repository
plan: 04
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - .planning/phases/03-persistent-supplier-repository/03-VERIFICATION.md
autonomous: false
requirements:
  - REPO-01
  - REPO-02
  - REPO-03
  - REPO-04
  - REPO-05
  - REPO-06
tags:
  - verification
  - phase-close
estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The full D-06 verification suite (`npm run typecheck && npm run lint && npm test && npm run build`) executes with a zero exit code on the merged state of Plans 03-01/02/03."
    - "The 225+ existing tests baseline established at Phase 1 close (see STATE.md 2026-08-15 blocker entry) is still green — no regression introduced by the repository additions."
    - "The two-org isolation test from Plan 03-01 passes as part of the full suite — REPO-04 acceptance criterion verified in CI, not just in isolation."
    - "The `supplier_identities` and `org_supplier_data` tables materialize successfully on Neon at cold-start (validated by a live `initSchema()` invocation on the production DATABASE_URL, or via a targeted script — see task below) — since raw SQL DDL passes typecheck/lint/build unconditionally, false-positive verification is possible without this live check (per contribution#schema-gate: this project uses raw SQL, not a listed ORM, so the standard ORM push detector does not fire; developer discretion adds this live check)."
    - "The end-of-phase human sign-off checkpoint confirms that no `@/lib/db` import was added to `lib/agents.ts`, no `INNER JOIN` was used in `lib/supplier-repository.ts`, and the two-org test's substring negation on `JSON.stringify(resultsForOrgA)` is present."
  prohibitions:
    - "Phase 3 MUST NEVER be marked complete without the two-org isolation test passing in the full suite (REPO-04 acceptance)."
    - "Phase 3 MUST NEVER be marked complete without `npm run build` succeeding (schema DDL syntax errors would surface here even without a live DB)."
    - "The final verification MUST NEVER skip the schema-applied check — because raw SQL DDL through `initSchema()` passes typecheck/lint/build regardless of whether the SQL is actually valid on Neon (per contribution#schema-gate: the generic ORM-push detector does not fire for this project, but the underlying concern — false-positive verification because compile checks bypass live-DB semantics — still applies to raw SQL)."
  artifacts:
    - .planning/phases/03-persistent-supplier-repository/03-VERIFICATION.md (new file — evidence log for D-06 verification suite + human sign-off record)
  key_links:
    - Plans 03-01/02/03 merged -> full suite green -> D-06 verification passes -> phase sealed
    - Two-org isolation test (Plan 03-01) -> REPO-04 acceptance criterion satisfied
---

# Phase 3 · Plan 04 — Final Verification + Schema-Applied Check + Phase Sign-Off (D-06)

## Artifacts this phase produces

- **New file:** `.planning/phases/03-persistent-supplier-repository/03-VERIFICATION.md` — evidence log

## Objective

Run the full D-06 verification suite (`npm run typecheck && npm run lint && npm test && npm run build`) against the merged state of Plans 03-01/02/03, run a live schema-applied check (developer discretion per pattern-mapper's note that this project uses raw SQL rather than an ORM push mechanism, so the standard schema-gate detector does not fire but the underlying false-positive-verification concern applies), and record the human sign-off closing Phase 3.

**Purpose:** D-06 is the phase-completion gate. The verification suite is the same one Phase 1 used (`npm run typecheck && npm run lint && npm test && npm run build`) — matches STATE.md 2026-08-15 recorded baseline of 225/225 tests.

**Output:** `03-VERIFICATION.md` with dated evidence (command outputs, test counts, human sign-off).

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-persistent-supplier-repository/03-CONTEXT.md
@.planning/phases/03-persistent-supplier-repository/03-01-SUMMARY.md
@.planning/phases/03-persistent-supplier-repository/03-02-SUMMARY.md
@.planning/phases/03-persistent-supplier-repository/03-03-SUMMARY.md

## Tasks

<tasks>

<task type="auto">
  <name>Task 1: Run full D-06 verification suite + live schema-applied check + write 03-VERIFICATION.md</name>
  <files>.planning/phases/03-persistent-supplier-repository/03-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/03-persistent-supplier-repository/03-01-SUMMARY.md, 03-02-SUMMARY.md, 03-03-SUMMARY.md (READ IN FULL — confirm all three plans are truly complete before starting verification; if any is missing, halt and surface)
    - lib/db.ts (READ the top-level `ensureInit()` / `initSchema()` block — confirm the new DDL is inline in the ddl template)
    - .planning/phases/01-rename-brand-migration/01-04-SUMMARY.md (READ — confirm the Phase-1-baseline verification-suite command shape and the recorded 225/225 test count that this phase must not regress from)
  </read_first>
  <action>
    **Step 1 — Full verification suite (D-06):** Run in order and capture full stdout+stderr for each:
    ```bash
    cd "/Users/zanotellipietro/Library/CloudStorage/OneDrive-TheBostonConsultingGroup,Inc/Desktop/Sourcing tool/sourceiq"
    npm run typecheck
    npm run lint
    npm test
    npm run build
    ```
    Record the exit code and last 20 lines of each. If ANY exits non-zero, HALT — do not write the verification report; instead surface the failure to the user with the failing command and its output for triage.

    **Step 2 — Live schema-applied check (developer discretion per contribution#schema-gate):** Since the project uses raw SQL via `lib/db.ts` `initSchema()` rather than an ORM push command, the standard schema-gate ORM detector does not fire. But the underlying concern — that build/type-check pass without ever executing the DDL against a live database, giving a false-positive verification — still applies. Add a targeted live check.

    Create a temporary Node script (`/tmp/verify-phase-3-schema.ts` or `scripts/verify-phase-3-schema.ts` — use `$TMPDIR` for the tempfile) that:
    - Imports `getDb` from `@/lib/db`
    - Calls `getDb()` (triggers `ensureInit()` -> `initSchema()`)
    - Issues `SELECT to_regclass('supplier_identities') AS t1, to_regclass('org_supplier_data') AS t2`
    - Prints result; exits 0 if both are non-null, exits 1 otherwise

    Run it with `DATABASE_URL` (or `POSTGRES_URL`/`NEON_DATABASE_URL` — whichever is configured) already exported. **DO NOT** attempt to source `.env.local` (STATE.md's carried-forward safety-block note: the harness denies any command that reads/sources `.env.local`). If the environment variable is NOT set in the current shell, halt and surface: the user must export it themselves before this task can complete.

    If the script confirms both tables exist, record the output. If not, HALT and surface.

    **Step 3 — Write `.planning/phases/03-persistent-supplier-repository/03-VERIFICATION.md`:**

    ```markdown
    # Phase 3 Verification — Persistent Supplier Repository

    **Date:** {ISO date}
    **Executor:** {agent id/timestamp}

    ## D-06 Verification Suite

    | Command | Exit | Tail (last relevant line) |
    |---------|------|---------------------------|
    | npm run typecheck | 0 | {last line} |
    | npm run lint | 0 | {last line} |
    | npm test | 0 | {N/N passed — must be ≥ 225} |
    | npm run build | 0 | {last line} |

    ## Live Schema-Applied Check

    Ran `verify-phase-3-schema.ts` against Neon DATABASE_URL.
    Result: `supplier_identities` regclass = {non-null}, `org_supplier_data` regclass = {non-null}.
    Exit code: 0.

    ## Two-Org Isolation Test (REPO-04 acceptance)

    `npx vitest run tests/supplier-repository.test.ts -t "two-org isolation"` — passed as part of full suite.

    ## Grep Gates

    | Gate | Result |
    |------|--------|
    | `grep -c "@/lib/db" lib/agents.ts` | 0 (RESEARCH.md Pitfall 1 anti-pattern absent) |
    | `grep -c "INNER JOIN" lib/supplier-repository.ts` | 0 (Pitfall 4 compliance) |
    | `grep -c "LEFT JOIN org_supplier_data" lib/supplier-repository.ts` | ≥ 1 |
    | `grep -nE "CREATE TABLE IF NOT EXISTS (supplier_identities|org_supplier_data)" lib/db.ts` | 2 lines |

    ## Requirement Traceability

    - REPO-01 (persistent org-scoped identity store): ✅ tables materialized, tracer test Test-1 pass
    - REPO-02 (single shared write path): ✅ all three `makeProcessSupplier*` functions call shared upsert helpers
    - REPO-03 (dedup on normName/domainOf reuse): ✅ tests Q2, D1, tracer Test-2 pass
    - REPO-04 (structural per-org isolation, explicit two-org test): ✅ tracer Test-4, Test-5 pass
    - REPO-05 (pre-search check before scout dispatch): ✅ Plan 03-03 Task 2, tests R1/R2/M1-M10
    - REPO-06 (per-org scope, not platform-wide): ✅ mandatory `orgId` parameter on all read/write helpers; UNIQUE index is `(org_id, norm_name)` not just `(norm_name)`

    ## Phase-Level Blockers Cleared

    - STATE.md's Phase 3 blocker "Per-org (not platform-wide) repository scope is the assumed default per PROJECT.md Out of Scope — confirm before schema design locks in" — RESOLVED. Schema locks per-org scope via `(org_id, norm_name)` UNIQUE index and `WHERE org_id = ?` on all read helpers.

    ## Human Sign-Off

    Pending Task 2's human-check checkpoint.
    ```
  </action>
  <verify>
    <automated>test -f "/Users/zanotellipietro/Library/CloudStorage/OneDrive-TheBostonConsultingGroup,Inc/Desktop/Sourcing tool/sourceiq/.planning/phases/03-persistent-supplier-repository/03-VERIFICATION.md" &amp;&amp; echo "OK" || echo "FAIL: 03-VERIFICATION.md missing"</automated>
    <automated>cd "/Users/zanotellipietro/Library/CloudStorage/OneDrive-TheBostonConsultingGroup,Inc/Desktop/Sourcing tool/sourceiq" &amp;&amp; grep -E "\|\s*npm run (typecheck|lint|test|build)\s*\|\s*0\s*\|" .planning/phases/03-persistent-supplier-repository/03-VERIFICATION.md | wc -l | awk '{ if ($1 == "4") { print "OK: all four D-06 commands recorded exit=0" } else { print "FAIL: expected 4 exit=0 rows, got " $1; exit 1 } }'</automated>
    <automated>cd "/Users/zanotellipietro/Library/CloudStorage/OneDrive-TheBostonConsultingGroup,Inc/Desktop/Sourcing tool/sourceiq" &amp;&amp; grep -c "REPO-0[1-6]" .planning/phases/03-persistent-supplier-repository/03-VERIFICATION.md | awk '{ if ($1 &gt;= 6) { print "OK: all 6 requirements traced" } else { print "FAIL: expected ≥6 REPO-N references, got " $1; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/03-persistent-supplier-repository/03-VERIFICATION.md` exists.
    - All four D-06 commands recorded with exit code 0.
    - Live schema-applied check output recorded (both tables materialized on Neon).
    - Requirement traceability section maps all six REPO-01 through REPO-06 to specific evidence.
    - Grep-gate table shows `@/lib/db` in `lib/agents.ts` = 0, `INNER JOIN` in `lib/supplier-repository.ts` = 0.
    - Full test count is ≥ 225 (Phase 1 baseline) + repository additions (roughly 9 + 3 + 3 + 10 + 2 = ~27 new).
  </acceptance_criteria>
  <precondition>Plans 03-01, 03-02, 03-03 all merged. Neon DATABASE_URL (or POSTGRES_URL/NEON_DATABASE_URL) exported in the current shell environment for the live schema-applied check step — the user must set this themselves per STATE.md's carried-forward .env.local safety-block note.</precondition>
  <reversibility rating="reversible">Verification artifact only; revert deletes the file.</reversibility>
  <done>
    - 03-VERIFICATION.md created with all four verification-suite exit codes = 0, schema-applied check confirming both tables, and full requirement traceability.
    - No regression on the 225-test Phase 1 baseline.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human sign-off on Phase 3 completion (D-06 human-verify-mode=end-of-phase gate)</name>
  <what-built>
    - Two new Postgres tables (`supplier_identities`, `org_supplier_data`) with the exact DDL from RESEARCH.md, materialized via `lib/db.ts` `initSchema()` on Neon cold-start.
    - `lib/supplier-repository.ts` — new file with 4 upsert/read helpers + 3 matching-heuristic helpers (7 exports total) + 3 types.
    - `lib/process-supplier.ts` — all three `makeProcessSupplier*` functions call the shared upsert helpers with best-effort try/catch, including enrichment second-write inside the two existing `enrichTask` closures.
    - `app/api/orchestrate/route.ts` — new pre-search block before `runOrchestrator()`, folding matched repository entries into `avoidNames`.
    - `tests/supplier-repository.test.ts` — new file with two-org isolation test (REPO-04 acceptance criterion) + dedup + best-effort + concurrency + matching-heuristic + pre-search harness (~24 tests total).
    - `tests/process-supplier.test.ts` + `tests/quick-scan.test.ts` — `fakeDb()` extended to recognize new SQL patterns; new deepen + quick-scan repository-write tests.
    - `.planning/phases/03-persistent-supplier-repository/03-VERIFICATION.md` — full verification-suite evidence.
    - `lib/agents.ts` — UNCHANGED (grep-enforced zero `@/lib/db` imports).
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/03-persistent-supplier-repository/03-VERIFICATION.md` end-to-end and confirm all four verification commands exited 0 and the live schema-applied check confirms both tables.
    2. Skim `tests/supplier-repository.test.ts`'s "REPO-04: org isolation" describe block and confirm the assertions include `expect(JSON.stringify(resultsForOrgA)).not.toContain("Org B's confidential note")` (structural substring negation, not just row count).
    3. Confirm no `@/lib/db` import was added to `lib/agents.ts` by running: `grep -n "@/lib/db" lib/agents.ts` — expect zero hits.
    4. Confirm no `INNER JOIN` is present in `lib/supplier-repository.ts` by running: `grep -n "INNER JOIN" lib/supplier-repository.ts` — expect zero hits.
    5. (Optional smoke test) Run a real `POST /api/orchestrate` for an org that has NO prior repository entries and confirm no regression on the existing scout flow; then run a second POST in the same category and check via the app UI or SSE log that the second run's scouts have the first run's supplier names in their avoid list (REPO-05 in action).
  </how-to-verify>
  <resume-signal>Type "approved" to seal Phase 3, or describe any issue found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Verification-only phase — introduces no new attack surface. Existing boundaries from Plans 03-01/02/03 unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-13 | Spoofing | Verification report fabricated without running commands | medium | mitigate | Automated grep gates on `03-VERIFICATION.md` structure (exit-code rows, requirement-ID rows). Human-verify checkpoint independently reads the file. |
| T-03-14 | Repudiation | Sign-off recorded without human presence | low | accept | Human-verify checkpoint gate=blocking — orchestrator halts until "approved" is typed by a real user. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new external packages installed this phase. |
</threat_model>

<verification>
- `03-VERIFICATION.md` exists with all D-06 evidence.
- Human sign-off received via checkpoint gate.
- Grep gates green.
</verification>

<success_criteria>
Phase 3 sealed. STATE.md updated (by the orchestrator, not this plan) to mark Phase 3 complete and Phase 4 unblocked.
</success_criteria>

<output>
Create `.planning/phases/03-persistent-supplier-repository/03-04-SUMMARY.md` when done, containing:
- The final test count from `npm test`
- Any items surfaced during the human-verify checkpoint that were dispositioned (approved-with-note, or triggering a follow-up plan)
- Confirmation that all six REPO-* requirements are traceable in `03-VERIFICATION.md`
</output>
