No external API integration: this phase adds two new internal Postgres tables (`supplier_identities`, `org_supplier_data`) via the existing `@neondatabase/serverless` driver already in use, extends the existing internal `lib/process-supplier.ts` write path with plain Postgres upserts, and adds a new internal pre-search read step inside `app/api/orchestrate/route.ts`. No new external service, SDK, or third-party API is called.
